# Active Context: Next.js Starter Template

## Current State

**Template Status**: ✅ SASAVOT streamer business card website implemented

The template is now a fully functional personal business card website for the streamer SASAVOT, built with Next.js 16, TypeScript, and Tailwind CSS 4. All sections are implemented with a dark theme (gray-950 base) and purple/violet streamer accent colors.

## Recently Completed

- [x] 2026-03-12: Fixed reverse-scroll corruption on storytelling Scene 1 in `src/features/storytelling/components/CinematicStorytelling.tsx`: the first scene's GSAP-driven DOM now stays mounted even when later scenes are active, preventing banner/media nodes from being recreated with stale timeline bindings when the user scrolls back upward.
- [x] 2026-03-12: Removed background keyword overlays from the first and last storytelling scenes in `src/features/storytelling/components/CinematicStorytelling.tsx`, so `ENTRY` no longer appears on Scene 1 and the final scene no longer shows its large keyword text either.
- [x] 2026-03-12: Restored the earlier smooth-scroll feel for the homepage Hero and the first storytelling scene in `src/features/storytelling/components/CinematicStorytelling.tsx`: re-enabled `Lenis` on desktop, returned the softer scroll scrub profile, and restored `anticipatePin` so entry into the pinned story section feels inertial again.
- [x] 2026-03-12: Smoothed the Scene 1 -> Scene 2 storytelling handoff in `src/features/storytelling/components/CinematicStorytelling.tsx`: the immediately previous scene now remains mounted and visible as a dimmed stack layer for one extra step instead of unloading the moment the next scene activates, giving the first transition a proper overlap while older scenes still get cleaned up.
- [x] 2026-03-12: Restored Scene 2 model clarity in `src/features/storytelling/components/CinematicStorytelling.tsx` after the cleanup pass: increased the React Three Fiber canvas DPR range and enabled explicit antialiasing so the brain hologram no longer renders with visibly degraded detail on desktop displays.
- [x] 2026-03-12: Tightened storytelling resource cleanup in `src/features/storytelling/components/CinematicStorytelling.tsx`: on desktop, once a scene is passed its heavy DOM is no longer rendered (background videos, Scene 2 canvas/console, Scene 3 poem layer, Scene 1 media overlays), and passed scenes are hidden instead of lingering in the stack, reducing memory/render pressure during the story flow.
- [x] 2026-03-12: Optimized the new desktop storytelling scene stack in `src/features/storytelling/components/CinematicStorytelling.tsx` and `src/features/storytelling/components/CinematicStorytelling.module.css`: removed expensive fullscreen `filter` animation from stacked scenes, moved depth dimming to the existing overlay layer, reduced stack shadow cost, and added paint/layout containment to lower rendering lag.
- [x] 2026-03-12: Changed the desktop storytelling scene handoff in `src/features/storytelling/components/CinematicStorytelling.tsx` and `src/features/storytelling/components/CinematicStorytelling.module.css` from full replace transitions to a stacked-window model: previous scenes now stay visible underneath as dimmed/scaled layers, creating a modal-like scene stack while the current scene stays on top.
- [x] 2026-03-12: Updated home navigation and replay UX. `src/components/Navbar.tsx` now shows a clickable logo in the left rail that restarts the site from the beginning, `Главная` scrolls to the top when already on `/`, `src/components/Hero.tsx` no longer exposes the intro button in the hero header, and a new end-of-page replay block in `src/components/IntroReplaySection.tsx` lets users watch the intro again from the bottom of the home page.
- [x] 2026-03-12: Refined Story Scene 2 clarity and interaction in `src/features/storytelling/components/CinematicStorytelling.tsx`, `src/features/storytelling/components/CinematicStorytelling.module.css`, and `src/features/storytelling/data/scenes.ts`: added a clearer intro card for the console/model flow, added a close `×` control for the console panel, softened/limited model rotation during command selection, and updated Scene 2 explanatory copy.
- [x] 2026-03-12: Expanded context on archive/rating pages in `src/features/twitch/components/TwitchVods.tsx`, `src/features/twitch/components/VodsShelf.tsx`, `src/features/twitch/components/ClipsShelf.tsx`, `src/features/ratings/components/StreamRatingsPage.tsx`, and `src/features/ratings/components/RatingCard.tsx` with explanatory headers, usage hints, and clearer stream metadata so both sections are easier to understand at a glance.
- [x] 2026-03-11: Reworked the home entrance flow in `src/components/Hero.tsx` and `src/components/Navbar.tsx`: the page now fades in, the left nav rail slides in, the hero sequence runs as `Добро пожаловать` -> large left-entering `глеб_слей_фото.svg` image -> typed `Глеб Борисович Орлов`, and the top-right controls were simplified to `WATCH` and `INTRO`.
- [x] 2026-03-11: Strengthened reload-screen readiness gating in `src/components/NavigationReloadSync.tsx`: on real browser reload the overlay now stays visible until the hydrated DOM settles, fonts are ready, and currently mounted images/videos reach a renderable state (`video.preload` is forced to `auto` during this phase). Only after that, plus the minimum visible time, is the reload screen hidden.
- [x] 2026-03-11: Updated Story Scene 3 interaction and background behavior in `src/features/storytelling/components/CinematicStorytelling.tsx` and `src/features/storytelling/components/CinematicStorytelling.module.css`: poem lines are no longer tied to scroll progress, each button click now replaces the previous line with exactly one next line, and on the final word `История` the scene fades darker while the button disappears. The `Начать заново` control is unlocked only after an upward scroll gesture, and the Scene 3 background video was simplified back to normal continuous looping while the scene is active.
- [x] 2026-03-11: Moved current-live-stream sync to the server/API layer. `src/server/streams/service.ts` now exposes `syncCurrentLiveStream()`, which pulls the active Twitch stream and upserts it into `streams`, and both `src/app/api/streams/route.ts` and `src/app/api/streams/ratings/route.ts` call it before returning data. As a result, the live stream appears in ratings even if the schedule page was never opened.
- [x] 2026-03-11: Fixed duplicate stream rows at the data layer. `scripts/dedupe-streams.mjs` now merges near-duplicate stream records within a 5-minute window when titles match or starts differ by at most 2 minutes, and one existing duplicate row was removed from the database. `src/server/streams/repository.ts` now applies the same near-duplicate merge logic during live/VOD upserts, so the same эфир is updated instead of inserted twice.
- [x] 2026-03-11: Fixed hydration mismatch introduced by the reload overlay: removed pre-hydration mutations of `<html>` attributes in `src/app/layout.tsx`, switched reload boot state to `window.__sasagramPageReloading` / `window.__sasagramPageReloadStartedAt`, and now reveal the overlay via an injected `<style>` tag that `src/components/NavigationReloadSync.tsx` removes after boot.
- [x] 2026-03-11: Added a visible full-screen reload overlay for normal browser refresh in `src/app/layout.tsx`, `src/components/NavigationReloadSync.tsx`, and `src/app/globals.css`: an inline head script marks reload navigation before hydration, a dedicated overlay panel with progress animation is rendered immediately, and the client sync component keeps it visible briefly during page boot so users see `Страница обновляется` instead of a frozen-looking screen.
- [x] 2026-03-11: Changed intro playback policy in `src/components/DisclaimerOverlay.tsx` and `src/components/Hero.tsx`: intro now auto-shows only once on the first site visit per browser via `localStorage` key `sasagram:intro-watched`, and the Hero section now includes a `Смотреть интро` button that reopens the overlay on demand through the `sasagram:open-disclaimer` window event.
- [x] 2026-03-11: Restored true full-page reload behavior across the app: `src/app/layout.tsx` now forces dynamic/no-store rendering and mounts `src/components/NavigationReloadSync.tsx`, which clears client API cache on reload and forces a real reload when the browser restores from BFCache; `src/components/DisclaimerOverlay.tsx` no longer skips the intro on page reload; added dedicated `/reload` route via `src/app/reload/page.tsx` + `src/components/ReloadPageClient.tsx` to show a reload screen, clear client cache, and perform a full route reload; added `Релоад` link to `src/components/Navbar.tsx`.
- [x] 2026-03-09: Fixed tab branding update in `src/app/layout.tsx` and app icon assets: tab title changed to `SASASITE`; favicon sources now point to app-level `src/app/icon.png` and `src/app/apple-icon.png` (copied from `public/assets/logo/лого_сайт.png`) with explicit `<head>` icon links and version query to bypass browser cache.
- [x] 2026-03-09: Refined footer support CTA visual weight in `src/components/Footer.tsx`: moved Telegram support/proposals link to the right edge and simplified it to low-contrast plain text (no border/background) so it does not draw attention.
- [x] 2026-03-09: Updated footer CTA wording in `src/components/Footer.tsx`: the only footer button now explicitly says `Telegram тех. поддержки и предложки для сайта`.
- [x] 2026-03-09: Reworked footer to a minimal contact-only layout in `src/components/Footer.tsx`: removed all branding/copyright/tag text and left only a single clickable Telegram button linking to `https://t.me/haworking`.
- [x] 2026-03-09: Updated footer and tab icon branding: `src/components/Footer.tsx` now shows dedicated technical support contact info with Telegram handle `@haworking` (`https://t.me/haworking`), and `src/app/layout.tsx` metadata now sets site tab icons (`icon`, `shortcut`, `apple`) to `public/assets/logo/лого_сайт.png`.
- [x] 2026-03-09: Limited animation simplification strictly to Story Scene 3 in `src/features/storytelling/components/CinematicStorytelling.tsx`: disabled Scene 3 background parallax motion (`scale/yPercent` locked to static values for index `2`) and switched poem line animation to optimized fade-only rendering (removed per-line vertical motion transform offset).
- [x] 2026-03-09: Updated Story scenes 3/4 in `src/features/storytelling/components/CinematicStorytelling.tsx` and `src/features/storytelling/data/scenes.ts` per UI simplification request: removed heavy transparent poem backdrop layer in Scene 3, removed `Scene 04` eyebrow from Scene 4, removed Scene 4 contacts CTA button, changed followers label from `Фолловеры Twitch` to `нас уже`, and updated Scene 4 title to `ИСТОРИЯ СТАНОВЛЕНИЯ СООБЩЕСТВА 141`.
- [x] 2026-03-09: Implemented safe performance optimizations without changing site behavior/animation logic: `src/components/About.tsx` now lazy-loads `CinematicStorytelling` via `next/dynamic` with `ssr: false` to remove heavy GSAP/Three payload from initial SSR path, `src/features/storytelling/components/CinematicStorytelling.tsx` now pauses React Three Fiber render loop outside active Scene 2 (`frameloop: "never"` when inactive), and `next.config.ts` enables `experimental.optimizePackageImports` for `@react-three/drei` to reduce client bundle overhead.
- [x] 2026-03-09: Fixed client-side crash in Scene 4 followers animation in `src/features/storytelling/components/CinematicStorytelling.tsx`: removed `react-slot-counter` usage from Story counter rendering and replaced it with a local `requestAnimationFrame`-driven animated numeric counter (cubic ease-out) that formats digits with `Intl.NumberFormat("ru-RU")`, preserving visual increment behavior while eliminating third-party runtime instability.
- [x] 2026-03-09: Refined contacts text hierarchy and live badge colors in `src/components/Contact.tsx`: removed all descriptive subtitle lines under top contact headings (keeping only link/value line), restored email value under the heading (`VOTVOPROS.13@yandex.ru`), and changed `LIVE` status badge color in `Смотреть также` cards to red.
- [x] 2026-03-09: Updated new contacts assets/text in `src/components/Contact.tsx`: Instagram card now uses local icon `/assets/logo/inst_logo.jpg`, email card now uses `/assets/icons/yandex_logo.png`, and email title changed to `Почта для жалоб или серьезных вопросов` with no subtext rendered under that heading.
- [x] 2026-03-09: Added two new contact links in `src/components/Contact.tsx`: Instagram (`https://www.instagram.com/gleb0413`) and complaint/serious-questions email (`mailto:VOTVOPROS.13@yandex.ru`), and updated the top contacts layout to a 6-card adaptive grid (`2 cols mobile / 3 cols desktop`) as part of a refreshed design.
- [x] 2026-03-09: Removed the `Полезные ссылки` heading from contacts section in `src/components/Contact.tsx` as requested.
- [x] 2026-03-09: Switched contacts page to a minimalist visual style in `src/components/Contact.tsx`: removed aggressive red-heavy accents and glow effects, replaced with neutral zinc palette, clean bordered cards, subtle hover states, and calmer typography while keeping full-screen no-scroll layout.
- [x] 2026-03-09: Redesigned contacts page readability in `src/components/Contact.tsx`: introduced a high-contrast glass panel (`bg-black/72` + blur), stronger card surfaces/borders for both link groups, brighter typography with clearer hierarchy, and hover accents so content remains legible over high-intensity red animated background.
- [x] 2026-03-09: Restyled schedule CTA `Оценить стримы` in `src/features/schedule/components/StreamSchedule.tsx` to red/black accent theme: black background with red border/text and brighter red hover state.
- [x] 2026-03-09: Improved visibility of the `Оценить стримы` CTA on schedule page in `src/features/schedule/components/StreamSchedule.tsx`: switched from low-contrast red-tinted ghost style to high-contrast solid white button with dark text and shadow so it remains readable over bright red animated backgrounds.
- [x] 2026-03-09: Removed dim overlays and enforced full-screen non-scrolling layout on secondary routes (`/schedule`, `/vods`, `/contacts`, `/watch`, `/rating`): pages now use `h-screen` with `overflow-hidden`, fixed background video without darkening layers, footer removed from schedule/vods/contacts route wrappers, and corresponding content sections were compacted to fit viewport height in `src/app/*/page.tsx`, `src/features/schedule/components/StreamSchedule.tsx`, `src/features/twitch/components/TwitchVods.tsx`, `src/components/Contact.tsx`, and `src/features/twitch/components/WatchLivePage.tsx`.
- [x] 2026-03-09: Updated Story Scene 3 background video in `src/features/storytelling/data/scenes.ts`: changed `bgVideoSrc` from `/assets/bg/толик_сцена_3.webm` to `/assets/bg/толик_сцена_3.2.webm`.
- [x] 2026-03-09: Hardened Scene 4 follower counter against runtime OOM in `src/features/storytelling/components/CinematicStorytelling.tsx`: extracted `react-slot-counter` into memoized `SceneFourFollowersCounter` so scroll-driven parent rerenders do not re-drive the counter animation, stabilized separator array reference, and prevented redundant follower-state updates when the fetched value has not changed.
- [x] 2026-03-09: Fixed potential storytelling runtime OOM queue buildup in `src/features/storytelling/components/CinematicStorytelling.tsx`: in `ScrollTrigger.onUpdate`, refs (`progressRef`, `activeSceneRef`) are now updated immediately before scheduling React updates, and state updates are wrapped in `startTransition` to prevent high-frequency scroll ticks from accumulating duplicate pending renders.
- [x] 2026-03-09: Optimized storytelling runtime performance in `src/features/storytelling/components/CinematicStorytelling.tsx` without changing scene behavior: reduced high-frequency scroll-driven React updates by quantizing/throttling `progress` state writes (`PROGRESS_STATE_EPSILON`, `PROGRESS_STATE_PRECISION`) and added active-scene-based video playback control (pause non-neighbor background videos and pause Scene 1 swap video outside Scene 1) to cut unnecessary decode/render load.
- [x] 2026-03-09: Updated Scene 04 narrative text and description animation in `src/features/storytelling/data/scenes.ts`, `src/features/storytelling/components/CinematicStorytelling.tsx`, and `src/features/storytelling/components/CinematicStorytelling.module.css`: removed the previous sentence about motion narrative, replaced Scene 4 description with multi-sentence copy, and added smooth staggered sentence-by-sentence fade-in for Scene 4 description lines when the scene becomes active.
- [x] 2026-03-09: Updated Scene 3 -> Scene 4 transition in `src/features/storytelling/components/CinematicStorytelling.tsx` and `src/features/storytelling/components/CinematicStorytelling.module.css`: the final poem word `История` now stays visible until the scene handoff (no per-line fade-out for the last line), and Scene 3 now darkens with a dedicated black dim layer during the transition to Scene 4.
- [x] 2026-03-09: Switched Story Scene 4 followers display to library-based digit-column animation using `react-slot-counter` in `src/features/storytelling/components/CinematicStorytelling.tsx` and `src/features/storytelling/components/CinematicStorytelling.module.css`: the Twitch followers number now rolls per column (`bottom-up`, sequential mode) on live updates, Scene 4 content is center-aligned, and counter/title typography uses the same `Audex` family as Scene 1 text 1/2 banners.
- [x] 2026-03-09: Updated Story Scene 4 copy and added live Twitch followers counter in `src/features/storytelling/data/scenes.ts`, `src/features/storytelling/components/CinematicStorytelling.tsx`, and `src/features/storytelling/components/CinematicStorytelling.module.css`: renamed Scene 4 title to `История становления сообщества`, fetched `/api/twitch` on mount with 60s refresh cadence, and rendered formatted followers count directly under the Scene 4 title.
- [x] 2026-03-09: Restored previous overall page/story scroll pacing by reverting Scene 3 weight in `src/features/storytelling/components/CinematicStorytelling.tsx` from `13.8` back to `4.6` (undoing temporary 3x stretch), returning the prior global scroll feel.
- [x] 2026-03-09: Stretched cinematic Scene 3 duration by exactly 3x in `src/features/storytelling/components/CinematicStorytelling.tsx` by increasing its scene weight in `SCENE_SCROLL_WEIGHTS` from `4.6` to `13.8` (other scene weights unchanged), so Scene 3 consumes triple scroll distance before transitioning.
- [x] 2026-03-09: Removed residual post-command scroll lag in Scene 2 (`src/features/storytelling/components/CinematicStorytelling.tsx`) by adding an all-commands-complete fast-release effect: once all hotspot commands are executed, pending terminal typing timers are cleared immediately and prompt line is finalized, preventing queued `setTimeout` text updates from competing with scroll/render during transition to Scene 3.
- [x] 2026-03-09: Unified cinematic scroll feel after Scene 2 in `src/features/storytelling/components/CinematicStorytelling.tsx` to match earlier sections: rebalanced Scene 3 weight (`10.8 -> 4.6`), reduced extra pre/post poem dead-zone (`0.18/0.2 -> 0.05/0.06`), softened per-line spacing (`SCENE_THREE_LINE_STEP: 1.26 -> 1.12`), and aligned 2->3 handoff profile with default transition behavior (`duration 0.34`, `shift 102`, `overlap 0`, `start alpha 1`) so scroll perception stays consistently smooth across scenes.
- [x] 2026-03-09: Optimized Scene 2 -> Scene 3 scroll responsiveness and Scene 3 poem pacing in `src/features/storytelling/components/CinematicStorytelling.tsx`: removed delayed/gesture-gated Scene 3 text reveal and switched to immediate progress-based readiness, reduced scrub inertia (`0.9 -> 0.55`) and tightened 2->3 transition timings (`duration 0.56 -> 0.38`, `shift 96 -> 72`, `overlap 0.04 -> 0.02`) so Scene 3 starts without perceived post-command lag, and slowed per-line poem progression by increasing Scene 3 weight (`8 -> 10.8`) plus introducing line step spacing (`SCENE_THREE_LINE_STEP = 1.26`) to prevent rapid line skipping on wheel scroll.
- [x] 2026-03-09: Matched Scene 3 text appearance timing to disappearance in `src/features/storytelling/components/CinematicStorytelling.tsx`: set `SCENE_THREE_LINE_FADE_IN_PORTION = 0.28` to equal `SCENE_THREE_LINE_FADE_OUT_PORTION = 0.28`, making line fade-in/fade-out symmetric.
- [x] 2026-03-09: Removed abrupt disappearances in Scene 3 (`src/features/storytelling/components/CinematicStorytelling.tsx`): added global edge envelope fade (`SCENE_THREE_EDGE_FADE_PORTION = 0.11`) applied to every poem line opacity, increased per-line fade-out softness (`hold = 0.62`, `fade-out = 0.28` for slight overlap), and replaced instant text-off on scene exit with delayed hide (`SCENE_THREE_EXIT_HIDE_DELAY_MS = 220`) to avoid hard cut.
- [x] 2026-03-09: Added explicit crossfade feel for Scene 2 -> Scene 3 in `src/features/storytelling/components/CinematicStorytelling.tsx`: Scene 2 keeps smooth eased fade-out while Scene 3 now fades in from low alpha (`SCENE_TWO_TO_THREE_FADE_IN_START_ALPHA = 0.08`) instead of appearing fully visible at transition start.
- [x] 2026-03-09: Added dedicated smooth transition profile for Scene 2 -> Scene 3 in `src/features/storytelling/components/CinematicStorytelling.tsx`: introduced longer transition duration (`0.56`), reduced vertical shift (`96`), small overlap start (`0.04`), and eased blend (`power1.inOut`) applied specifically at the 2->3 boundary to make entry into Scene 3 noticeably smoother.
- [x] 2026-03-09: Extended Scene 3 timeline around poem and removed Scene 2 post-command tail delays in `src/features/storytelling/components/CinematicStorytelling.tsx`: added dedicated pre/post text portions (`SCENE_THREE_PRE_TEXT_TIMELINE_PORTION = 0.18`, `SCENE_THREE_POST_TEXT_TIMELINE_PORTION = 0.2`) so Scene 3 has more scroll time before first line and after the poem ends; removed extra completion waits after command execution in Scene 2 by dropping the final `+160ms` and panel toggle `+120ms` tail pauses.
- [x] 2026-03-09: Changed Scene 3 reveal trigger in `src/features/storytelling/components/CinematicStorytelling.tsx`: on entering Scene 3 the poem now stays hidden, and text appears only after a new user scroll gesture (`wheel`/`touchmove`) inside Scene 3; added a short arm delay (`SCENE_THREE_SCROLL_ARM_DELAY_MS = 180`) to avoid carry-over reveal from transition momentum.
- [x] 2026-03-09: Optimized Scene 2 performance and post-command scroll responsiveness in `src/features/storytelling/components/CinematicStorytelling.tsx`: removed heavy drei `Environment` HDR load, reduced 3D canvas DPR to `[1, 1.35]`, lowered scroll scrub inertia (`SCENE_PARALLAX_SCRUB` from `1.35` to `0.9`), and moved terminal typing updates into `startTransition` via `updateSceneTwoTerminal` helper to reduce main-thread jank during/after command playback.
- [x] 2026-03-09: Removed Scene 2 completion scroll-lock in src/features/storytelling/components/CinematicStorytelling.tsx: deleted unlock-gated clamp/forced scroll rollback and scene-index clamp logic, so after commands the timeline transitions smoothly to Scene 3 with normal scroll smoothing only (Lenis + scrub).

- [x] 2026-03-09: Added delayed Scene 3 poem reveal trigger in src/features/storytelling/components/CinematicStorytelling.tsx: text now starts rendering only after a short timer (SCENE_THREE_TEXT_REVEAL_DELAY_MS = 320) once Scene 3 becomes active/fixed, and resets when leaving Scene 3.

- [x] 2026-03-09: Removed Scene 3 internal scroll inertia in `src/features/storytelling/components/CinematicStorytelling.tsx`: replaced eased/held sequence progress (`smootherStep` + start/end holds) with direct linear mapping `clamp01(sceneThreeRawProgress)` so poem lines track scroll input immediately.

- [x] 2026-03-09: Added initial Scene 3 line-start delay in `src/features/storytelling/components/CinematicStorytelling.tsx`: introduced `SCENE_THREE_INITIAL_LINE_DELAY = 0.55` and shifted poem line progress so the first line appears later at scene start.

- [x] 2026-03-09: Increased Scene 3 per-line on-screen hold in `src/features/storytelling/components/CinematicStorytelling.tsx`: switched poem line opacity to explicit phased timing (`fade-in 22% -> hold 56% -> fade-out 22%`) and increased Scene 3 scroll weight from `5` to `8` so lines remain fully visible longer and progress more slowly during scroll.

- [x] 2026-03-09: Fixed Scene 2 scroll lock behavior in `src/features/storytelling/components/CinematicStorytelling.tsx`: lock clamp now applies only while scrolling down (`self.direction > 0`) so upward scroll has no resistance, and Scene 2 unlock flag is set immediately on the final command click to prevent one-frame jitter before releasing to Scene 3.

- [x] 2026-03-09: Enforced capped and consistent cinematic scroll pacing: set equal scene weights (`[1,1,1,1]`) for uniform per-scene animation speed, reduced fixed wheel step (`90`) and increased wheel throttle interval (`90ms`) to cap max scroll speed independent of mouse intensity, and increased Lenis duration (`1.3`) for smoother motion in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Added wheel-event throttling for steadier cinematic scroll (`FIXED_WHEEL_INTERVAL_MS = 70`) on top of fixed wheel step normalization, so rapid mouse-wheel bursts no longer accelerate scroll unpredictably in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Added fixed wheel-step normalization in Story Lenis setup (`virtualScroll`) using constant `FIXED_WHEEL_STEP = 120`, making desktop scroll progression more consistent regardless of mouse wheel delta amplitude in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Extended Scene 3 hold further by increasing Scene 3 scroll weight from `8` to `16`, so each line remains visible significantly longer on normal wheel scroll in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Extended Scene 3 duration without changing perceived fade speed: increased Scene 3 weight to `8` and added fade auto-compensation (`fadePortion = 0.34 * 5 / currentWeight`) so line fade-in/out pace stays close to previous while on-screen hold increases in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Reverted global scroll rebalance to restore original page scroll feel: reset cinematic settings back to `SCENE_SCROLL_LENGTH_MULTIPLIER = 2` and `SCENE_SCROLL_WEIGHTS = [3.6, 2.2, 30, 1]`, preserving the extended Scene 3 duration while returning normal overall scroll behavior in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Rebalanced cinematic scroll pacing to restore Scenes 1/2/4 speed while keeping Scene 3 duration approximately unchanged: adjusted global scroll multiplier to `3.37` and scene weights to `[17, 10.4, 30, 4.7]` in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Lengthened Scene 3 timeline for slower text lifecycle: increased Scene 3 scroll weight to `30` and restored longer fade envelope (`0.3 / 0.3`) so each line appears slowly, stays longer, and disappears slowly during scroll in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Increased Scene 3 line hold substantially by reducing fade portions to `0.16 / 0.16`, so each line spends most of its slot fully visible before disappearing in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Slowed Scene 3 scroll pacing further by increasing its timeline weight from `12` to `18`, making wheel-scroll progression significantly slower specifically in Scene 3 (`SCENE_SCROLL_WEIGHTS`) in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Further increased Scene 3 per-line on-screen time by extending Scene 3 scroll weight from `8` to `12` (fade envelope unchanged), so each poem line persists longer during normal wheel scroll in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Restored Scene 3 fade envelope to previous values (`fade in/out = 0.34/0.34`) and lengthened Scene 3 overall timeline by increasing its scroll weight to `8`, so lines stay on screen longer due to expanded scene duration rather than hold-only tweaks in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Increased Scene 3 per-line on-screen hold time by reducing fade portions to `0.2 / 0.2`, so each line stays fully visible longer before fade-out in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Extended per-line Scene 3 fade timeline by increasing both `fade in` and `fade out` portions to `0.34`, making each line appearance/disappearance animation longer and smoother in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Increased Scene 3 per-line on-screen hold during scroll: replaced symmetric quick fade with phased envelope (`fade in -> hold -> fade out`) so each line remains readable longer under normal wheel scrolling in `src/features/storytelling/components/CinematicStorytelling.tsx`


- [x] 2026-03-09: Added adaptive centering for selected long Scene 3 lines (`И смотрит по ту сторону стекла.`, `Это характер по ту сторону воды.`, `Пузырьки поднимаются со дна.`, `И среди этого плавает рыбка.`): those lines now use a dedicated width-constrained balanced-wrap style while keeping center alignment in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`


- [x] 2026-03-09: Reworked Scene 3 into sequential line fades: removed wipe/cursor/mask mechanics and switched to centered one-line-at-a-time animation where each poem line smoothly fades in then fades out; text size aligned with Scene 1 top/bottom large banners (`scrollBanner`/`scrollBannerSecondary`) in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`


- [x] 2026-03-09: Fixed Scene 3 wipe-mask regression: removed local `--line-reveal-progress: 0` override from `.sceneThreePoemSymbolLayer` so gray masks inherit per-line reveal progress and erase left-to-right again in `src/features/storytelling/components/CinematicStorytelling.module.css`


- [x] 2026-03-09: Removed Scene 3 symbol glyph overlay and switched to uniform gray per-line mask: each poem row now uses the same fixed-width gray block that wipes away left-to-right to reveal Russian text, with consistent row lengths and no symbol rendering in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`

- [x] 2026-03-09: Fixed Scene 3 symbol rendering overlap: moved reveal progress variable to line scope and split visibility masks so Russian base text is clipped to translated area while symbol SVG layer is clipped to untranslated area, preventing Russian bleed-through under symbols in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`

- [x] 2026-03-09: Reduced Scene 3 symbol density to match OSOS-like aesthetics: symbol overlay now generates compact Japanese Katakana blocks per word (instead of per-character substitution), removed forced SVG text stretching, and increased spacing for cleaner visual rhythm in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`

- [x] 2026-03-09: Updated Scene 3 symbol overlay fidelity: switched scramble set to Japanese Katakana symbols, increased symbol spacing/width normalization in SVG (`textLength` + `lengthAdjust="spacing"`), and made overlay layer near-opaque so Russian base text is not visible before wipe in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`

- [x] 2026-03-09: Reworked Scene 3 decode visual to layered wipe model: Russian poem text is now the base layer, and pseudo-Asian symbols are rendered as a dedicated SVG overlay that is erased left-to-right by scroll progress (cursor is the wipe boundary), matching the “erase image to reveal text” behavior in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Synced Scene 3 glyph conversion boundary with cursor position: switched resolved-char threshold to ceil(cursor progress) so the character under the sweep cursor is already translated when cursor reaches it in src/features/storytelling/components/CinematicStorytelling.tsx
- [x] 2026-03-09: Adjusted Scene 3 translation style to reference behavior: full poem is now always visible as pseudo-Asian symbols first and scroll only converts symbols into Russian text (no per-line appearance during scroll), while the whole poem block itself fades in once on scene entry via `activeScene === 2` in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Slowed Scene 3 poem animation significantly for mouse-wheel scroll by increasing Scene 3 timeline weight in cinematic storytelling (`SCENE_SCROLL_WEIGHTS[2] = 5`), so cursor/text decoding advances much more gradually per scroll input in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-09: Fixed Scene 3 sweep-cursor motion and pacing: cursor horizontal movement now binds to line width (`left: calc(progress * 100%)`) so it visibly runs across text, added stronger start delay before poem decoding begins, and slowed Scene 3 decode-per-scroll by increasing Scene 3 scroll weight plus softer easing in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Refined Scene 3 cursor behavior to match reference style: replaced inline blinking caret with a separate fixed vertical sweep cursor that smoothly moves across the active line while progressively resolving scrambled glyphs into final text; also made scrambled symbols deterministic/static for cleaner translation effect in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Tuned Scene 3 poem pacing/layout: added stronger start/end hold (delays) with smoother slow cursor progression, reduced scramble update frequency for less jitter, and expanded poem backdrop/text bounds so full multiline poem fits the area more reliably on desktop/mobile in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Enhanced Story `Scene 03` poem FX: added centered blurred/dimmed backdrop plate behind text, switched unrevealed characters to deterministic random glyphs, and implemented a scroll-driven moving cursor that progressively resolves scrambled symbols into final poem text in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Updated Story `Scene 03` poem behavior to typewriter-style reveal by letters with accumulating lines: text now gradually builds into a full readable poem by end of scene (no line fade-out), plus blinking caret on active line and centered multiline layout in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Added global `@font-face` for `Preciosa` (`/assets/font/Кириллические/Preciosa/Preciosa.ttf`) and applied it to Scene 3 poem text styling in `src/app/globals.css` and `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-09: Reworked cinematic Story `Scene 03` in `CinematicStorytelling`: removed the content/card and keyword overlay for scene index `2`, kept only background video, and added centered sequential poem lines (fade in/out per line) with new scene-3 poem styles in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-09: Set cinematic Story `Scene 03` background video to `public/assets/bg/толик_сцена_3.webm` by adding `bgVideoSrc: "/assets/bg/толик_сцена_3.webm"` in `src/features/storytelling/data/scenes.ts`
- [x] 2026-03-07: Restyled Scene 2 command controls into larger explicit button UI: commands now render as black buttons with gray border, increased padding/width/spacing, and updated hover/active states for stronger CMD-like readability in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Added Scene 2 model hover cursor affordance: 3D canvas now shows `grab` on hover and `grabbing` on active drag for clearer interaction feedback in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Updated Scene 2 CMD deduplication logic: commands can be pressed in any order, but each command is written to terminal history only once per scene session (no duplicate terminal entries for repeated presses, including `start/end`) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Updated Scene 2 `end` command behavior: after `> end` execution the selected hotspot is cleared, right CMD plaque closes, and the model returns to baseline autonomous rotation from its default pose (no active-point highlight/focus retained) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Simplified Scene 2 hotspot behavior: removed selected-point callout line/text overlay entirely so only the hotspot point itself changes visual state on selection, and blocked duplicate same-command execution back-to-back (for both brain commands and `start/end`) to prevent repeated identical terminal input in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Refined Scene 2 selected-hotspot callout readability by adding per-point line/text layout profiles (`frontal`, `temporal`, `parietal`, `occipital`): each hotspot now has custom diagonal angle, line lengths, and label offsets so callout text stays visible and does not overlap model center; also improved callout text plate contrast in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Adjusted Scene 2 panel trigger behavior: right CMD plaque no longer opens on scene start (default closed), opens only via command interaction (`> start`), and `start/end` command items now reveal progressively with the same timed sequence logic as other Scene 2 commands in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Added Scene 2 control commands `> start` and `> end` in the left command list: `start` opens the right CMD plaque and `end` closes it; both commands are typed into terminal input as prompt lines (`C:\brain>...`) using the same per-line insertion flow as other commands in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Scene 2 terminal and model UX refined per follow-up: removed connector line from selected brain point to right panel, converted right panel to full black CMD plaque with a strong gray separator and no nested inner window, changed command execution to append each new command as a separate new prompt line (`C:\brain>...`) with streamed output below, and kept/enabled slow auto-rotation even while the model is focused/enlarged in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Reworked Scene 2 terminal UX/layout: replaced closable floating popup with a right-side slide-in full-height command plaque (wider, navbar-like styling), kept left command list intact, switched command click behavior to typed insertion into terminal prompt (`C:\brain>...`) before output/details stream, and added selected-point brain-part marker line/label near the active hotspot while preserving head rotation logic in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Updated Scene 2 command interaction so an already open command window no longer resets on new text click: command output now appends as a new block in the same window (with separator) instead of clearing previous lines; also expanded panel-attach recalculation dependencies (`terminalLines` + typed-details length) to keep connector alignment stable while content grows and model rotates in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Updated Scene 2 execution output to CMD-like commands via new `execCommand` per brain region (e.g. `brain_scan --region ...`) while keeping left labels human-readable; also localized all `status` output lines to Russian (`status: все исправно`) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Updated Scene 2 command output status line from `status: online` to `status: fully operational` across all four brain-region commands in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Rewrote all Scene 2 `info` detail texts into formal “analysis was conducted” wording with per-region findings explicitly tied to brain regions of Глеб Борисович in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Removed the leading phrase `Анализ мозга Глеба Борисовича Орлова:` from all Scene 2 `info` details while keeping the rest of each region description unchanged in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Rewrote Scene 2 hotspot descriptions and command-output context for all four brain regions with explicit focus on “анализ мозга Глеба Борисовича Орлова” (added subject line + region-specific functional summaries) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Simplified Scene 2 command input labels to short Russian brain-part names (`Лобная доля`, `Височная доля`, `Теменная доля`, `Затылочная доля`) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Localized Scene 2 command labels to Russian functional-check phrasing for all four brain-part commands (`проверка ... -- функциональность`) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Updated Scene 2 command labels from `text_1..text_4` to English brain-part functional checks (`check frontal-lobe|temporal-lobe|parietal-lobe|occipital-lobe --functional`) and aligned node output labels to the same naming in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Restyled Scene 2 command checkboxes to match command text style and edge alignment: removed outer capsule area, increased checkbox size, kept monospace command font feel, aligned checks to sidebar edge, and removed glow on completed state in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Replaced Scene 2 `Команд выполнено` text with four command-linked checkbox indicators: each checkbox now turns green with a check mark when its corresponding command is executed (`sceneTwoChecks`, `sceneTwoCheckItem`, `sceneTwoCheckDone`) in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Added local darkening behind Scene 2 model by layering a central radial black gradient over the square grid backdrop, reducing grid visibility through the semi-transparent model silhouette in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Updated Scene 2 focus rotation behavior to minimize fixed-panel connector length: selected hotspot rotation is now computed from hotspot position (`atan2(z, x)`) toward the panel side, and residual auto-rotation is damped out on selection to avoid random offset in line distance (`getMinimalConnectorRotationY`) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Center-aligned Scene 2 square grid backdrop to model axis by replacing clamp-based offsets with explicit stage center anchoring (`left: 50%`, `top: 50%`, `translate(-50%, -50%)`) in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Synced Scene 2 `OrbitControls.target` with active model focus position (`focusY/focusZ`) instead of static base target, so manual rotation axis stays aligned with the current centered model position across angled/bottom views in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Fixed Scene 2 model rotation pivot drift by auto-centering GLB content around its bounding-box center in `BrainHologramModel`: applied a shared center offset group for mesh+hotspots and synchronized projected line anchor calculations with that offset, so manual/auto rotation uses the true model center in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Tuned Scene 2 white grid square framing to reduce oversized top/right extents: shifted grid anchor slightly left/down and reduced square size (`left/top/width/height` clamps) for a visually even square around the model in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Refined Scene 2 grid visual style to hard-edged white square grid: changed line color from cyan to white, removed glow/inset shadows, set black fill, and added a crisp 1px white border for uniform straight edges in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Constrained Scene 2 model grid backdrop to a compact square behind the model only (not full-height): switched to fixed square dimensions (`width/height: clamp(320px, 34vw, 620px)`) and nudged anchor position for better alignment in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Repositioned Scene 2 square grid backdrop to sit directly behind the model by changing `.sceneModelGridBackdrop` anchoring from right-offset to center-anchor (`left + translate(-50%, -50%)`) and slightly enlarging the backdrop area in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Added a square local grid backdrop behind the Scene 2 3D model: inserted `.sceneModelGridBackdrop` layer (repeating linear-grid pattern) under the model canvas while keeping the overall stage black in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Scene 2 reset to black-only background with no cursor illumination: removed spotlight DOM node, pointer move/leave spotlight handlers, and spotlight CSS blocks, leaving only the base black stage for Scene 2 in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Scene 2 cursor spotlight now reveals a dedicated image background `public/assets/bg/фон_сцена_2.png` only inside the cursor halo over a black base using CSS mask (`mask-image`/`-webkit-mask-image` with `--mx/--my`), plus a soft cyan glow ring around the cursor in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Added cursor-following spotlight layer for Scene 2 background: an overlay radial glow now tracks pointer coordinates (`--mx/--my`) across the scene stage to highlight area around cursor, with mobile fallback disabled (`display: none`) in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Simplified Scene 2 connector to a single direct line anchored from the projected selected hotspot to the fixed right command panel (removed elbow intermediate point); line endpoint now targets panel mid-height for stable visual attachment in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Refined Scene 2 connector behavior to stay locked to the selected 3D point during mouse rotation: line start is now frame-synced from projected hotspot screen coordinates (`useFrame` + `Vector3.project`) while the command panel remains fixed on the right; also removed dynamic model scale shifts so size stays constant during scroll/focus in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Implemented command-driven Scene 2 focus workflow with fixed HUD: clicking `> text_*` now rotates/zooms model to target point, stops autonomous rotation during focus, enables user drag-rotation via `OrbitControls`, and renders line+description as a right-fixed command-style overlay independent from model movement in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Reworked Scene 2 left controls into pseudo-CMD interaction: `text_1..text_4` now render as command entries prefixed with `>`, and clicking a command triggers staged terminal-like output lines in a dedicated response panel while preserving model focus switching (`runSceneTwoCommand`) in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Updated Scene 2 focus-line shape to match reference geometry using animated SVG polyline (`point -> diagonal up -> long horizontal`) with delayed panel reveal under the top horizontal segment; replaced previous straight connector implementation in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Sequenced Scene 2 focus-callout reveal animation: on hotspot selection the connector line now animates first, then the transparent description panel appears with a delayed fade/slide-in (`sceneTwoFocusLineReveal` -> `sceneTwoFocusPanelReveal`) in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Added click-driven focus callout panel in Scene 2: selecting a left-side item (including `Текст 1`) now shows a line from the corresponding hotspot point and a semi-transparent description area attached to that line (`Html` overlay with `sceneTwoFocusLine` + `sceneTwoFocusPanel`) in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Tuned Scene 2 model presentation: increased model scale, shifted model anchor to the right side of frame, and added slow continuous auto-rotation (`autoRotationRef`) while preserving click-to-focus behavior in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Reworked Scene 2 interaction flow: left-side test controls `1/2/3/4` are hidden initially and reveal sequentially on scene entry with matching hotspot points on the model; removed scroll-driven model motion, and clicking a left item now smoothly rotates/repositions the model camera target toward the corresponding point (focus turn + slight zoom-in) in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Tuned Scene 2 hotspot callouts to match side-offset reference layout: significantly extended the horizontal elbow segment and anchored detail text position to the segment endpoint, so labels/descriptions render to the right side (off the model silhouette) instead of overlapping the mesh (`--callout-horizontal-length`) in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Implemented two-step Scene 2 hotspot semantics based on reference behavior: hover shows elbow callout (`45deg` then `90deg`) with short label, and click toggles an extended descriptive paragraph for that hotspot under the line endpoint (`selectedHotspot` + `hotspotDetails`) in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Updated Scene 2 hotspot callout geometry to an elbow path: on hover, line now first extends at `45deg` from the point, then turns by `90deg` into a horizontal segment, and the label appears at the end of that horizontal segment (`hotspotLineDiagonal` + `hotspotLineHorizontal`) in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-07: Refined Scene 2 hotspot UX to minimal callouts: only small dots are permanently visible on the model, and on hover each dot now emits a thin rightward line with text appearing along it (removed prior background label plate/area) in `src/features/storytelling/components/CinematicStorytelling.tsx` and `CinematicStorytelling.module.css`
- [x] 2026-03-07: Enhanced cinematic `Scene 02` 3D interaction by slightly increasing model size and adding 4 hover hotspots on the brain model; each hotspot now reveals an animated UI callout (line slides out + label fades/translates in) via `Html` overlays and new CSS motion styles in `src/features/storytelling/components/CinematicStorytelling.tsx` / `CinematicStorytelling.module.css`
- [x] 2026-03-07: Updated cinematic `Scene 02` 3D behavior to be scroll-driven: model now starts slightly smaller, performs a subtle intro "approach" at scene entry, and rotates proportionally to Scene 2 scroll progress (instead of autonomous rotation). Also increased Scene 2 timeline weight from `1` to `2.2` in `SCENE_SCROLL_WEIGHTS` to allow longer scroll observation in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-07: Locked cinematic `Scene 02` 3D model camera interaction to fully static presentation by removing `OrbitControls` from the Scene 2 canvas in `src/features/storytelling/components/CinematicStorytelling.tsx`; `brain_hologram.glb` no longer auto-rotates or zooms in/out during scroll
- [x] 2026-03-07: Refined cinematic `Scene 02` 3D presentation to model-only view: removed visual stage/container framing around `brain_hologram.glb` (no border, panel, glow box) and switched `Scene 02` background to pure black (`linear-gradient(180deg, #000 0%, #000 100%)`) in `src/features/storytelling/components/CinematicStorytelling.module.css` and `src/features/storytelling/data/scenes.ts`
- [x] 2026-03-07: Cleared all textual/UI content from cinematic `Scene 02` and replaced it with a dedicated 3D viewer rendering `public/assets/3d/brain_hologram.glb` using `@react-three/fiber` + `@react-three/drei` (`Canvas`, `useGLTF`, `Bounds`, `OrbitControls`, `Environment`) in `src/features/storytelling/components/CinematicStorytelling.tsx` with new model-stage styling in `CinematicStorytelling.module.css`
- [x] 2026-03-07: Stopped Scene 01 background parallax exactly at Scene 02 entry point by making Scene 01 bg tween duration equal its scene weight (without extra `+0.2` overlap) in `src/features/storytelling/components/CinematicStorytelling.tsx`; later scenes keep existing slight overrun smoothing
- [x] 2026-03-07: Refined Scene 01 second-media dimming to local focus area only: `.cupSwapDim` changed from full-scene overlay to centered radial vignette around photo/text (desktop+mobile tuned sizes/position), and dim intensity increased (`autoAlpha 0.62`) in `CinematicStorytelling.tsx` / `CinematicStorytelling.module.css`
- [x] 2026-03-07: Added smooth background dim layer for Scene 01 second media phase: introduced `.cupSwapDim` overlay in `CinematicStorytelling.module.css` and timeline-controlled fade-in/out in `CinematicStorytelling.tsx` synced to `swapAt`, so when `глеб_слей_сцена1.webm` appears the scene background darkens gradually behind it
- [x] 2026-03-07: Further raised Scene 01 Text 4 (`2025`) by an additional 10px: `.scrollBannerQuaternary` base `top` offset changed from `- 5px` to `- 15px` in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Raised Scene 01 Text 4 (`2025`) base position by 5px (reduced top offset) by updating `.scrollBannerQuaternary` `top` calc with `- 5px` in `src/features/storytelling/components/CinematicStorytelling.module.css`
- [x] 2026-03-07: Restored Scene 01 Text 4 (`2025`) swap-phase vertical shift on second media reveal in `CinematicStorytelling.tsx`: when `глеб_слей_сцена1.webm` appears, Text 4 now moves down with the same `SCENE_TEXT_UNDER_MEDIA_SHIFT_Y` tween as Text 3; exit fade sync with Text 3 remains unchanged
- [x] 2026-03-07: Re-synced Scene 01 center text exits in `CinematicStorytelling.tsx`: restored missing Text 3 (`SLAY KING`) handoff fade tween and aligned Text 4 (`2025`) fade to the same timestamp as Text 1/2 edge-exit start (`at - SCENE_TEXT_EDGE_EXIT_LEAD`); removed extra Text 4 fade motion so both center texts now disappear identically
- [x] 2026-03-07: Advanced Scene 01 Text 1/2 edge-exit timing slightly earlier by adding `SCENE_TEXT_EDGE_EXIT_LEAD = 0.12` and starting both edge-out tweens at `at - lead`, so the two banners leave the frame before the next scene handoff begins
- [x] 2026-03-07: Extended Scene 01 post-swap hold and aligned progress markers with weighted timeline: increased Scene 01 weight (`3.6`) and added explicit hold segment after second media reveal (`SCENE_SWAP_MEDIA_HOLD_DURATION = 0.45`); progress bar step positions now derive from weighted scene start anchors instead of equal spacing
- [x] 2026-03-07: Smoothed cinematic background parallax to reduce visible scroll jitter in Scene flow: reduced parallax amplitude (`scale 1.06 -> 1`, `yPercent 1.5 -> -1.5`), increased scrub smoothing (`1.35`), disabled rounding in timeline defaults, set `immediateRender: false` for bg `fromTo`, and added GPU composition hints (`translateZ(0)`, `backface-visibility`, `will-change`) to `.sceneBg` / `.sceneVideo`
- [x] 2026-03-07: Extended Scene 01 first-photo hold time before media swap: introduced `SCENE_CUP_HOLD_DURATION = 0.75` and delayed `swapAt` accordingly, so `cup.png` remains visible longer before transitioning to `глеб_слей_сцена1.webm`
- [x] 2026-03-07: Nudged Scene 01 Text 3/4 swap-phase vertical placement up by 10px by reducing `SCENE_TEXT_UNDER_MEDIA_SHIFT_Y` from `160` to `150` in `CinematicStorytelling.tsx`
- [x] 2026-03-07: Reverted Scene 01 second-media z-layer back to original (`.cupBackdropSwap z-index: 4`) and switched to Y-position approach: Text 3/4 now move below photo by increased swap-phase shift (`SCENE_TEXT_UNDER_MEDIA_SHIFT_Y = 160`) in `CinematicStorytelling.tsx`
- [x] 2026-03-07: Updated Scene 01 second-media layering per UX tweak: raised `.cupBackdropSwap` to `z-index: 7` so Text 3/4 render beneath the second photo/video layer without altering user-tuned media position/size values
- [x] 2026-03-07: Increased Scene 01 second media (`глеб_слей_сцена1.webm`) size by ~30% without affecting `cup.png`: introduced dedicated `.cupBackdropSwap` class and applied it only to the swap video node in `CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Added Scene 01 media swap phase after Text 4 reveal: `cup.png` now fades out and `глеб_слей_сцена1.webm` fades in at the same anchor point, while Text 3 (`SLAY KING`) and Text 4 (`2025`) both animate down by `20px` (temporary tuning value) in `CinematicStorytelling.tsx`
- [x] 2026-03-07: Fixed Scene 01 layering issue where `cup.png` covered Text 3: lowered `.cupBackdrop` stacking level (`z-index: 4`) below `.scrollBannerTertiary` so `SLAY KING` always renders above the cup image while preserving user-tuned image position/size
- [x] 2026-03-07: Implemented Scene 01 Text 4 + image reveal sequence in cinematic storytelling: after `SLAY KING`, centered `2025` now appears below Text 3 and `cup.png` fades/scales in behind it; both are timeline-driven via new refs (`quaternaryBannerRefs`, `cupRefs`) and are faded out during scene handoff
- [x] 2026-03-07: Matched Scene 01 tertiary text scale to primary/secondary banners by applying identical responsive size formulas (`calc(clamp(...) + 12px)`) for `.scrollBannerTertiary` on desktop and mobile in `CinematicStorytelling.module.css`
- [x] 2026-03-07: Unified Scene 01 Text 1/2 edge offsets to one shared rule: both banner endpoints now use the same `SCENE_TEXT_EDGE_PADDING` with computed style-aware X calculations (`getNumericStylePx`) so left/right resting margins are consistent across both moving texts
- [x] 2026-03-07: Tightened right-edge safe spacing for Scene 01 banners: increased motion endpoint edge padding (`SCENE_TEXT_EDGE_PADDING` from `24` to `36`) and reduced banner max width (`100% - 3.5rem` desktop / `100% - 2rem` mobile) in `CinematicStorytelling.tsx` + `.module.css` to prevent right-side clipping/touching
- [x] 2026-03-07: Corrected left-edge offsets for Scene 01 banners by removing duplicated side-rail compensation: reverted banner CSS `left/max-width` to scene-local bounds (`100%`-based) and removed extra rail subtraction/addition from banner GSAP X calculations in `CinematicStorytelling.tsx`, fixing over-shift to the right
- [x] 2026-03-07: Introduced global left-rail safe area and applied it to Scene 01 moving banners: added `--left-rail-offset` root var in `globals.css`, now updated by `Navbar` alongside body padding; storytelling banner CSS positions/max-widths and GSAP X-bound calculations in `CinematicStorytelling.tsx` now subtract the rail offset so text motion remains inside visible content area instead of drifting under/offscreen due to persistent side rail
- [x] 2026-03-07: Updated Scene 01 Text 1/2 typography/edge layout for filled rendering and readability: switched both moving Cyrillic banners from outline-style Sigma to filled `Audex`, increased their responsive size by `+12px` (`calc(clamp(...) + 12px)`), added explicit scene-edge offsets in CSS, and aligned motion endpoints with edge padding via `SCENE_TEXT_EDGE_PADDING` in `CinematicStorytelling.tsx`
- [x] 2026-03-07: Added requested local typefaces for Scene 01 banners: registered `sigmafivecyrilic2.ttf` (`Sigma Five Cyrillic 2`) and `Hassianuncial.ttf` (`Hassian Uncial`) in `src/app/globals.css`; applied Sigma font to Text 1/2 (`.scrollBanner`, `.scrollBannerSecondary`) and Hassian font to Text 3 (`.scrollBannerTertiary`) in `CinematicStorytelling.module.css`
- [x] 2026-03-07: Refined Scene 01 tertiary text trigger semantics in cinematic storytelling: `SLAY KING` reveal is now explicitly tied to completion of Text 1/Text 2 travel (`SCENE_TEXT_TRAVEL_DURATION + 0.02`) instead of a standalone absolute offset
- [x] 2026-03-07: Fixed Scene 01 tertiary text trigger timing in cinematic storytelling: `SLAY KING` is now spawned inside Scene 01 end-phase (`at + 1.15`) via opacity-only reveal (no transform override), instead of being tied to Scene 01->02 handoff, which previously prevented visible centered appearance
- [x] 2026-03-07: Added Scene 01 tertiary text beat in cinematic storytelling: after primary/secondary texts drive to opposite edges during scene outro, centered text `SLAY KING` now appears in the middle of Scene 01 with matching typography scale; implemented via `tertiaryBannerRefs` timeline cue and new `.scrollBannerTertiary` style in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`
- [x] 2026-03-07: Added end-of-scene text drive-out in cinematic storytelling (`CinematicStorytelling.tsx`): during scene handoff, Scene 01 primary text now continues moving further to the right edge-offscreen while secondary text continues to the left edge-offscreen, creating a directional outro before the next scene enters
- [x] 2026-03-07: Normalized Scene 01 secondary text parameters to match primary text in `CinematicStorytelling`: `Мэр города Курска` now uses the same animation/profile settings as `ВЕЛИКИЙ ХОУБИСТ` (same opacity model, 1-unit travel duration, and matching visual style values), while keeping only intended differences in content, position, and travel destination
- [x] 2026-03-07: Updated Scene 01 dual-text timing/scale in cinematic storytelling: secondary text `Мэр города Курска` now starts immediately with primary text (same timeline start and 1-unit travel), uses the same responsive font sizing as `ВЕЛИКИЙ ХОУБИСТ`, and Scene 01 scroll duration was extended to 3x via weighted pacing (`SCENE_SCROLL_WEIGHTS = [3,1,1,1]`) in `src/features/storytelling/components/CinematicStorytelling.tsx` + CSS
- [x] 2026-03-06: Added dual-text choreography for cinematic `Scene 01` in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`: primary text `ВЕЛИКИЙ ХОУБИСТ` continues existing left-to-right motion, and secondary text `Мэр города Курска` now enters from the bottom-right side once the first text reaches mid-phase (`at + 0.5`) and travels to the left edge
- [x] 2026-03-06: Updated homepage-only startup behavior: `DisclaimerOverlay` now makes visibility decision after mount and skips intro loading on hard reload (`PerformanceNavigationTiming.type === "reload"` + legacy fallback) in `src/components/DisclaimerOverlay.tsx`; added `src/components/HomePageScrollReset.tsx` and mounted it only in `src/app/page.tsx` to force `/` route to start at top (Hero) with `history.scrollRestoration = "manual"`
- [x] 2026-03-06: Constrained cinematic scene backgrounds to scene bounds in `CinematicStorytelling.module.css` by adding `overflow: hidden` to `.scene` and resetting `.sceneBg` from `inset: -2px` to `inset: 0`, preventing next-scene background top bleed into the previous scene
- [x] 2026-03-06: Fixed premature Scene 02 visibility during Scene 01 hold in cinematic storytelling by disabling eager GSAP `fromTo` render for next-scene entry (`immediateRender: false` on scene entry tween) in `src/features/storytelling/components/CinematicStorytelling.tsx`
- [x] 2026-03-06: Fixed persistent bottom-edge next-scene bleed during Scene 01 transition: removed full-scene overscan (`.scene` inset reverted to `0`) and widened handoff translation bounds in `CinematicStorytelling.tsx` (`-102/102` instead of `-100/100`) so Scene 02 cannot appear as a strip above the progress bar before the actual transition point
- [x] 2026-03-06: Refined Scene 01 -> Scene 02 handoff visibility in cinematic storytelling: removed transition overlap offsets (`yPercent` back to `-100/100`) in `CinematicStorytelling.tsx` so next scene no longer peeks before switch, and added vertical scene overscan (`.scene { inset: -2px 0; }`) in `CinematicStorytelling.module.css` to keep transitions seam-safe
- [x] 2026-03-06: Fixed thin black-strip artifact above storytelling progress bar during Scene 01 -> Scene 02 handoff by adding transition overlap margin (`yPercent: -99/99` instead of `-100/100`) in `CinematicStorytelling.tsx` and slight scene background overscan (`.sceneBg { inset: -2px; }`) in `CinematicStorytelling.module.css`; also set a distinct test gradient for `Scene 02` in `src/features/storytelling/data/scenes.ts` to visually validate scene switching
- [x] 2026-03-06: Restabilized cinematic storytelling transitions in `src/features/storytelling/components/CinematicStorytelling.tsx` after multi-step regression: removed ratio-based per-scene phase scheduler and replaced it with deterministic per-index timeline flow where each next scene enters from bottom while previous exits upward at the same timestamp, with synchronized content/keyword/banner fade-outs to avoid blank/black stage intervals and broken section sequencing
- [x] 2026-03-06: Fixed black-gap regression between cinematic scenes in `src/features/storytelling/components/CinematicStorytelling.tsx`: scene stack now keeps `autoAlpha: 1` and scene entry starts during previous scene exit (`enterAt = holdAt - enterDuration`) to create seamless vertical handoff without empty black intervals
- [x] 2026-03-06: Repaired cinematic scene flow after transition regression in `src/features/storytelling/components/CinematicStorytelling.tsx`: timeline was simplified to fixed per-scene segments with deterministic `entry (from bottom) -> hold -> exit (to top)` phases, each scene now consumes exactly one timeline unit (no empty gaps), and active-scene thresholds were normalized to scene count
- [x] 2026-03-06: Reworked cinematic scene transitions to explicit phase flow in `src/features/storytelling/components/CinematicStorytelling.tsx`: each scene now has `entry -> pinned hold -> exit up`, next scene starts only after previous scene exit, and per-scene active/progress thresholds were aligned to phase durations (instead of flat per-scene weights)
- [x] 2026-03-06: Unified cinematic scene-scroll behavior in `src/features/storytelling/components/CinematicStorytelling.tsx`: removed delayed start for scenes `02+` so their transitions start like Scene 01, and enabled ScrollTrigger scene snapping (`snapTo` by scene count) for consistent section-to-section scroll switching
- [x] 2026-03-06: Adjusted cinematic scene transition pacing/stacking in `src/features/storytelling/components/CinematicStorytelling.tsx`: removed 2x weight for `Scene 01` (now baseline timing), and changed timeline ordering so each next scene fades in only after the previous scene fades out, eliminating top-overlay transition feel
- [x] 2026-03-06: Hardened `Scene 01` banner/transition rendering in `src/features/storytelling/components/CinematicStorytelling.tsx`: banner end position now uses actual scene width (not `window.innerWidth`) with right safety padding, background parallax vertical offsets were removed (`yPercent: 0`) to prevent black-edge flashes, and `ScrollTrigger.refresh()` now runs after `document.fonts.ready` for correct post-font layout measurements
- [x] 2026-03-06: Fixed cinematic `Scene 01` visual regression in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`: removed previous-scene upward transform during fade-out to prevent black background exposure, kept banner fade-out in place (no off-screen push), and reduced responsive banner size so `ВЕЛИКИЙ ХОУБИСТ` fits within viewport
- [x] 2026-03-06: Updated cinematic `Scene 01` layout in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`: removed `Scene 01 / Entry Point / description` content block entirely for the first scene, moved `ВЕЛИКИЙ ХОУБИСТ` to start from the top-left edge, and tuned responsive banner sizing so it stays within viewport bounds
- [x] 2026-03-06: Fixed `Scene 01` banner start timing and visual container style in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`: `ВЕЛИКИЙ ХОУБИСТ` now starts sliding from the first pinned moment (removed conflicting `xPercent` offset), and the highlighted card area was removed for Scene 01 content via `contentPlain`
- [x] 2026-03-06: Increased cinematic storytelling `Scene 01` scroll duration by 2x in `src/features/storytelling/components/CinematicStorytelling.tsx` using weighted scene timing (`SCENE_SCROLL_WEIGHTS`), while keeping subsequent scenes at baseline length
- [x] 2026-03-06: Refined `Scene 01` cinematic banner behavior in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`: `ВЕЛИКИЙ ХОУБИСТ` now stays in a single line (`white-space: nowrap`), starts moving immediately when the pinned scene begins, and scroll-drives all the way to the right screen edge
- [x] 2026-03-06: Added a large scroll-driven callout to cinematic storytelling `Scene 01` in `src/features/storytelling/components/CinematicStorytelling.tsx` / `.module.css`: text `ВЕЛИКИЙ ХОУБИСТ` now slides horizontally into view and continues moving with the pinned GSAP scroll timeline
- [x] 2026-03-06: Fixed `/watch` Twitch chat not loading on client-side navigation in `src/features/twitch/components/WatchLivePage.tsx`: embed hostname is now set after mount via `useEffect`, player/chat iframes are keyed by resolved host to force correct remount, and iframe loading switched to `eager` to prevent lazy-load stalls after SPA transitions
- [x] 2026-03-06: Increased cinematic storytelling scroll distance per section on desktop in `src/features/storytelling/components/CinematicStorytelling.tsx` by raising the pinned timeline length multiplier from `1.35` to `2` (`SCENE_SCROLL_LENGTH_MULTIPLIER`), so each scene requires more scroll before transition
- [x] 2026-03-06: Switched requested background videos to new `public/assets/bg` assets: `Scene 01` in cinematic storytelling now supports per-scene video backgrounds and uses `Фон_раздел_2.webm` (`src/features/storytelling/data/scenes.ts`, `src/features/storytelling/components/CinematicStorytelling.tsx`, `src/features/storytelling/components/CinematicStorytelling.module.css`); fullscreen menu right-column video in `src/components/Navbar.tsx` now uses `edit_navbar_slay.webm`
- [x] 2026-03-06: Fixed persistent burger visibility issue in side-rail nav by moving the toggle button out of the rail stacking context into a top-level fixed layer (`z-[130]`) in `src/components/Navbar.tsx`; button now remains visible and clickable above fullscreen menu overlay and continues to toggle open/close on the same control
- [x] 2026-03-06: Replaced static burger icon with animated `Burger menu.lottie` in side rail navigation trigger (`src/components/Navbar.tsx`), added client-side `@dotlottie/player-component` registration and React JSX custom-element typing in `src/types/dotlottie-player.d.ts`
- [x] 2026-03-06: Removed decorative vertical `SASAVOT - CREATIVE - STORY` label from fullscreen navigation menu right column in `src/components/Navbar.tsx` per UX request
- [x] 2026-03-06: Added Gropled typography support in `src/app/globals.css` (`Gropled Bold` + `Gropled Hollow`) and updated fullscreen nav tab states in `src/components/Navbar.tsx` to match OSOS-like active/inactive behavior: active item uses filled bold style, inactive items use outlined hollow style with stroke; active state follows current route and interactive hover/focus override
- [x] 2026-03-06: Rebuilt navigation architecture per OSOS-style request in `src/components/Navbar.tsx`: removed the top capsule navbar, introduced an always-visible fixed left side rail overlay (above all content), offset page content from the rail via dynamic body padding, and implemented fullscreen navigation that slides from the top only when the burger is clicked (fully hidden off-canvas when closed)
- [x] 2026-03-06: Redesigned mobile burger navigation to an OSOS-inspired fullscreen overlay in `src/components/Navbar.tsx`: screen-wide panel with cinematic dark backdrop, left utility rail + close control, large numbered menu rows, decorative side typography, smooth entrance transition, and body-scroll lock while menu is open
- [x] 2026-03-06: Optimized homepage network behavior by disabling Next.js link prefetch for main navigation and heavy CTA transitions (`Navbar`, `Hero`, and cinematic storytelling CTA links), preventing eager preloading of secondary pages while users stay on the home route
- [x] 2026-03-06: Updated homepage/background behavior per UX request: removed the shared fixed video background layer after Hero on `/`, kept video background only inside Hero itself, removed "История" from navbar links, and set `/story` route to immediate redirect to `/` (no standalone story page content)
- [x] 2026-03-06: Added dedicated story route `/story` (`src/app/story/page.tsx`) with pure black background and no shared site video layer; updated Navbar "История" link to `/story` so cinematic storytelling is isolated from homepage video backgrounds
- [x] 2026-03-06: Replaced custom chapter-scroll implementation with a modular cinematic storytelling engine for `/` using `Lenis + GSAP ScrollTrigger` (`src/features/storytelling/*`): pinned 100vh scenes, scrub-driven scene transitions, per-scene transform/opacity/parallax motion, active-scene state, and fixed segmented scroll progress bar; `src/components/About.tsx` is now a thin wrapper over `CinematicStorytelling`, and scene content is configurable through `src/features/storytelling/data/scenes.ts`
- [x] 2026-03-06: Expanded pinned storytelling from 5 to 8 chapters in `src/components/About.tsx` by adding three new stages (`Rating`, `Backstage`, `Final checkpoint`), updated chapter numbering (`01..08`), and added matching background themes in `src/app/globals.css` (`.story-flow-theme-rating`, `.story-flow-theme-backstage`, `.story-flow-theme-finale`)
- [x] 2026-03-06: Implemented true chapter-pinned storytelling flow in `src/components/About.tsx`: each chapter has its own sticky viewport stage and extended scroll length, text lines reveal progressively within the pinned chapter, chapter transitions happen only after local reveal completion, fixed bottom segmented progress bar now follows total story scroll, and active chapter theme switches the background (`.story-flow-theme-*` in `src/app/globals.css`)
- [x] 2026-03-06: Fixed storytelling scroll mechanics in `src/components/About.tsx`: migrated from plain stacked sections to a true pinned/sticky chapter scene (`height = steps * 100vh` + sticky viewport panel), implemented rAF-based normalized scroll progress tied to the section container, rendered a fixed segmented bottom progress bar with active checkpoints, and switched chapter background themes based on checkpoint index (`.story-pin-theme-*` in `src/app/globals.css`)
- [x] 2026-03-06: Reworked homepage storytelling into scroll-driven chapter panels (`src/components/About.tsx`): each story block is a separate full-screen section, active chapter is detected by viewport control point, and a fixed segmented progress bar was added at the bottom of the screen; background now changes by chapter checkpoint via theme classes in `src/app/globals.css` (`.story-scroll-theme-*`)
- [x] 2026-03-06: Redesigned the homepage storytelling section (`src/components/About.tsx`) toward an editorial interactive style inspired by `osos.webflow.io`: oversized display typography, animated marquee strip, high-contrast chapter stage with large keyword panel, route-choice pills, and chapter mood theming; added full supporting stylesheet block in `src/app/globals.css` (`.oso-*` classes + `osoMarquee` animation)
- [x] 2026-03-06: Restructured homepage information architecture: moved functional sections to dedicated routes (`/schedule`, `/vods`, `/contacts`) with their own page entries in `src/app/*/page.tsx`, updated `Navbar` links to route-based navigation, and simplified root `/` to Hero + interactive story + Footer
- [x] 2026-03-06: Replaced the About placeholder with an interactive storytelling chapter flow (`src/components/About.tsx`) featuring branching story nodes, progress tracking, back-navigation through story history, and contextual CTA transitions to `/watch`, `/rating`, `#schedule`, `#vods`, and `#contact`; added dedicated visual theme system for story chapters in `src/app/globals.css` (`.story-shell`, progress bar, and chapter-specific background atmospheres)
- [x] 2026-03-06: Physically removed duplicate rows from `streams` via `scripts/dedupe-streams.mjs` (1 duplicate row deleted, duplicate minute groups reduced from 1 to 0), merged ratings from duplicate stream IDs, and enforced minute precision with DB check constraint `streams_started_at_minute_precision_check`
- [x] 2026-03-06: Added duplicate-prevention normalization for `started_at` to minute precision in ingest paths (`src/server/streams/service.ts`, `scripts/import-streams-2026.mjs`, `scripts/migrate-to-supabase.mjs`)
- [x] 2026-03-06: Fixed missing current live rating in schedule cards by correcting same-day stream filtering in `buildWeeks` (`startedAt > now` instead of `startedAt > today`) and by reusing DB rating fields (`ratingAvg`, `ratingCount`) in the live-card branch when a persisted stream exists
- [x] 2026-03-05: Extended rating-card video preview to side edges on mobile by adding horizontal negative margins (`-mx-5`) with top flush alignment, while keeping desktop layout unchanged (`lg:mx-0 lg:rounded-2xl`) in `src/features/ratings/components/RatingCard.tsx`
- [x] 2026-03-05: Removed top gap above video preview banner in rating cards on mobile by applying `-mt-5` to preview container (`lg:mt-0` preserved desktop layout) in `src/features/ratings/components/RatingCard.tsx`
- [x] 2026-03-05: Reduced mobile gap under rating header/burger on `/rating` by changing top offset to `pt-[56px]` (desktop unchanged `md:pt-[92px]`) and matching section height to `h-[calc(100vh-56px)]` on mobile
- [x] 2026-03-05: Centered WeekPicker dropdown on mobile by positioning calendar popover at `left-1/2 -translate-x-1/2` and preserving desktop behavior with `md:left-0 md:translate-x-0` in `src/features/schedule/components/WeekPicker.tsx`
- [x] 2026-03-05: Hid visible scrollbar in rating stream list while preserving scroll behavior by adding reusable `.hide-scrollbar` utility in `src/app/globals.css` and applying it to the `overflow-y-auto` container in `StreamRatingsPage.tsx`
- [x] 2026-03-05: Increased transparency of the main rating panel container in `StreamRatingsPage` by changing background from `bg-[#09090b]/90` to `bg-[#09090b]/75` for a lighter, more see-through overlay effect
- [x] 2026-03-05: Positioned mobile `Рейтинг стримов` label as a fixed top-left overlay (`left-4 top-2`, `md:hidden`) on `/rating`, matching burger-menu offsets and removing the in-flow duplicate label
- [x] 2026-03-05: Scoped `/rating` copy/layout changes to mobile only: restored desktop (`md+`) hero block (`Рейтинг стримов` + `Оцените стримы` + explanatory text) while keeping mobile header simplified and top-aligned as requested
- [x] 2026-03-05: Updated `/rating` hero copy/layout: removed `Оцените стримы` title and explanatory paragraph, and anchored `Рейтинг стримов` at the top with burger-matching offsets (`px-4 pt-2` mobile, `md:px-6 md:pt-3`) in `src/app/rating/page.tsx`
- [x] 2026-03-05: Refined `/rating` mobile layout: removed the in-panel heading text (`Выберите неделю и оцените реальные стримы`), centered the week calendar controls in `StreamRatingsPage`, and removed mobile outer section paddings (`px-0 pb-0` with desktop-only `md:px-6 md:pb-6`) so the rating area sits flush with screen edges on phones
- [x] 2026-03-05: Simplified mobile shelf headers: removed all top controls above carousels on phones (prev/next/action buttons), and restored section titles only on mobile via `md:hidden` labels (`Записи стримов`, `Избранные клипы`) in `VodsShelf.tsx` and `ClipsShelf.tsx`
- [x] 2026-03-05: Removed shelf section labels (`Записи стримов`, `Избранные клипы`) and hid top-right `Все видео` / `Все клипы` header buttons on desktop (`lg:hidden`) in `VodsShelf.tsx` and `ClipsShelf.tsx`; mobile header actions remain available
- [x] 2026-03-05: Limited Twitch shelves to 7 media items each at UI layer (`vods.slice(0, 7)` and `clips.slice(0, 7)` in `TwitchVods.tsx`) while preserving current carousel behavior and terminal CTA banners
- [x] 2026-03-05: Removed unintended extra side banners in Twitch shelves by reverting Swiper viewport to default clipping (`overflow-hidden` via removing `!overflow-visible`), and widened shelf containers to `max-w-[1320px]` so side cards remain less clipped without exposing additional off-screen slides
- [x] 2026-03-05: Prevented side-banner clipping in Twitch shelves without resizing cards by setting Swiper viewport to `overflow-visible` (`className="!overflow-visible ..."`) in both `VodsShelf.tsx` and `ClipsShelf.tsx`
- [x] 2026-03-05: Fixed prev-button visibility conflict in Twitch shelves: replaced `disabled={!canPrev}` with `aria-disabled={!canPrev}` for prev buttons so Tailwind `disabled:opacity-35` no longer leaks a visible faded button; visibility now fully controlled by `canPrev` with `opacity-0` + `pointer-events-none`
- [x] 2026-03-05: Added progressive visibility for previous navigation buttons in Twitch shelves: on first slide the "prev" buttons are visually hidden, and they fade in (`transition-opacity`) once the user moves forward (applied to both mobile header and desktop overlay buttons in `VodsShelf.tsx` / `ClipsShelf.tsx`)
- [x] 2026-03-05: Updated Twitch shelves per UX request: removed UI slicing to 3 items (now `TwitchVods` passes full `vods`/`clips` arrays), removed DB read limits (`LIMIT 20`) from `getCachedMediaState`, forced Swiper startup at first slide (`slideTo(0, 0, false)` + `initialSlide={0}`), disabled loop for predictable edge behavior, and added terminal CTA slides `Все видео` / `Все клипы` as the last banners in each shelf
- [x] 2026-03-05: Stabilized animation behavior for 3-card Twitch shelves by disabling loop for `items.length <= 3` (`isLoop = items.length > 3`) and restoring native `slidePrev/slideNext` controls in both `VodsShelf.tsx` and `ClipsShelf.tsx`; this brings back normal slide animation and removes reverse-direction loop artifacts
- [x] 2026-03-05: Fixed reverse navigation animation direction in Twitch shelves: replaced direct `slidePrev/slideNext` loop navigation with `slideToLoop` based on `realIndex` in `VodsShelf.tsx` and `ClipsShelf.tsx`, so clicking back animates backward instead of visually moving forward
- [x] 2026-03-05: Fixed regressions in Twitch coverflow after limiting to 3 items: desktop slide width switched from percent-based to fixed (`xl:!w-[500px]`), slider container widened to `max-w-[1240px]`, and `watchOverflow` removed in both `VodsShelf.tsx` and `ClipsShelf.tsx` to preserve normal swipe/arrow behavior
- [x] 2026-03-05: Limited Twitch media shelves to 3 banners each (VODs + Clips) in `src/features/twitch/components/TwitchVods.tsx` and narrowed both slider containers with `max-w-[1120px]` in `VodsShelf.tsx` / `ClipsShelf.tsx` while preserving existing card/video dimensions (no stretch)
- [x] 2026-03-05: Tuned coverflow shelves sizing: desktop slide widths now target 3 visible cards per row, coverflow stretch/depth reduced for cleaner composition, and VOD section row heights increased to prevent bottom clipping of banner content
- [x] 2026-03-05: Updated VOD/Clips Swiper logic to match coverflow-style carousel behavior (`centeredSlides`, `slidesPerView: auto`, `effect: coverflow`, `watchSlidesProgress`) and added progress-based dynamic side dimming overlays (active slide brighter, side slides darker)
- [x] 2026-03-05: Reworked VOD and Clips shelves to true Swiper-based horizontal carousels (`swiper/react`) with breakpoint-driven `slidesPerView`, keyboard support, smooth slide animation, and existing custom left/right overlay controls wired via Swiper API
- [x] 2026-03-05: Enlarged side overlay pagination controls for both VOD and Clips shelves and added left/right desktop arrows directly on card area edges; header arrows are now mobile-only (`lg:hidden`) in `src/features/twitch/components/VodsShelf.tsx` and `src/features/twitch/components/ClipsShelf.tsx`
- [x] 2026-03-05: Moved the existing VOD pagination `next` control from the header action row into an overlay position at the right-center of the cards area in `src/features/twitch/components/VodsShelf.tsx` (same behavior, new placement)
- [x] 2026-03-05: Removed the extra overlay swipe button from `src/features/twitch/components/VodsShelf.tsx`; recordings pagination now uses only the existing top navigation arrows next to `Все видео`
- [x] 2026-03-05: Added a dedicated right-side swipe button for VOD pagination in `src/features/twitch/components/VodsShelf.tsx`, positioned at the vertical center of the cards area (desktop `lg+`) and bound to the existing `onNext` page action
- [x] 2026-03-05: Reverted SASAVOT avatar source back to the original circle video `Кружок_сасыч.webm` in both `src/components/Hero.tsx` and `src/components/Navbar.tsx` (removed temporary MP4 fallback source)
- [x] 2026-03-05: Improved mobile avatar autoplay reliability: added MP4 fallback source (`sasych_head.mp4`) next to WebM for SASAVOT avatar videos in `src/components/Hero.tsx` and `src/components/Navbar.tsx`, enabled autoplay for the proposal avatar video in `src/components/Contact.tsx`, and limited hover-only play/pause handlers to devices that actually support hover
- [x] 2026-03-05: Removed test scroll page feature: deleted route `src/app/fullpage-test/page.tsx`, deleted unused component `src/components/FullpageTestView.tsx`, and removed `Тест скролла` navigation item from `src/components/Navbar.tsx`
- [x] 2026-03-05: Reverted `src/app/page.tsx` to the previous homepage composition (Navbar, Hero, About, StreamSchedule, TwitchVods, Contact, Footer) so the main `/` screen no longer uses the temporary stacking-scroll test layout
- [x] 2026-03-05: Fully removed `fullpage.js` from project dependencies/integration and replaced `/fullpage-test` with native sticky overlay section scrolling (section layering effect) in `src/components/FullpageTestView.tsx`; also removed fullpage CSS/type declarations and renamed navbar entry to `Тест скролла`
- [x] 2026-03-05: Added isolated fullPage.js playground route `/fullpage-test` with near-doc config (`anchors`, `navigationTooltips`, `slidesNavigation`, `scrollingSpeed: 609`, `fitToSectionDelay: 600`, `controlArrows: false`, `rtl: false`, `effects: "cover"`) and env-driven keys (`NEXT_PUBLIC_FULLPAGE_EFFECTS_KEY`, `NEXT_PUBLIC_FULLPAGE_LICENSE_KEY`)
- [x] 2026-03-05: Switched site-wide default body text font to Ubuntu via `next/font/google` in `src/app/layout.tsx` and applied global base font-family in `src/app/globals.css`; decorative font utility classes (`Audex`, `Them People`, `Type Light Sans`, `Fontick`) remain intact for explicit use-cases
- [x] 2026-03-05: Removed live floating mini-player (PiP) entirely from `src/components/Hero.tsx`, moved Twitch player+chat block to dedicated page `/watch` (`src/app/watch/page.tsx`, `src/features/twitch/components/WatchLivePage.tsx`), and added conditional blinking red `Смотреть` nav item in `src/components/Navbar.tsx` that appears only while stream is active
- [x] 2026-03-05: Restored Hero live rendering in `src/components/Hero.tsx`: when Twitch is live, Hero now shows the visible stream player + optional chat panel again (instead of empty/incorrect state), and floating PiP live player rendering with drag/resize/close controls is active while scrolling below Hero
- [x] 2026-03-05: Reverted `fullPage.js` cover-scroll experiment and restored prior homepage state (regular section flow + existing desktop arrow navigation/wheel lock); removed `fullpage.js` dependency from the project
- [x] 2026-03-05: Added desktop-only section navigation controls on homepage (`src/components/DesktopSectionNavigator.tsx`) with fixed up/down arrows that smoothly move between `#home`, `#about`, `#schedule`, `#vods`, `#contact`; also disabled mouse-wheel page scrolling on desktop to make section transitions arrow-driven and more interactive
- [x] 2026-03-05: Removed all star glow/filter effects in desktop schedule stream cards and kept plain `star_rait.png` icon only in `src/features/schedule/components/ScheduleDesktop.tsx`
- [x] 2026-03-05: Removed the `Рейтинг` label from desktop schedule stream cards and replaced the inline SVG with icon asset `public/assets/icons/free-icon-star-2164323.png` in `src/features/schedule/components/ScheduleDesktop.tsx`
- [x] 2026-03-05: Moved rating in desktop schedule stream cards to the right side and increased its size for stronger emphasis in `src/features/schedule/components/ScheduleDesktop.tsx`
- [x] 2026-03-05: Removed the large top schedule highlight strip (title/time/rating preview banner) from `src/features/schedule/components/StreamSchedule.tsx`; schedule section now shows only the standard weekly calendar/timeline UI
- [x] 2026-03-05: Extended schedule data pipeline to include stream rating aggregates (`ratingAvg`, `ratingCount`) from `/api/streams`, and rendered these ratings directly inside desktop/mobile schedule cards so votes from `/rating` are visible in расписание
- [x] 2026-03-05: Added global pointer UX for clickable buttons in `src/app/globals.css` (`button:not(:disabled) { cursor: pointer; }`) and explicit disabled cursor (`not-allowed`) to keep non-interactive states clear
- [x] 2026-03-04: Added anonymous stream rating flow on dedicated route `/rating`: real streams are fetched from `streams` with aggregate rating stats, voting happens through `/api/streams/ratings` and `/api/streams/[id]/rating`, policy is one vote per stream per browser/device without edits, and the server uses a random `HttpOnly` cookie token hashed with SHA-256 before persistence
- [x] 2026-03-04: Added DB bootstrap for `stream_ratings` with `UNIQUE (stream_id, viewer_token_hash)`, `CHECK rating BETWEEN 1 AND 5`, `ON DELETE CASCADE`, and `stream_id` index; the implementation explicitly avoids storing name, email, raw IP, account identity, or browser fingerprint
- [x] 2026-03-04: Added lightweight rating entrypoints without polluting the homepage schedule UI: Navbar now links to `/rating`, schedule header has a compact `Оценить стримы` CTA, and the dedicated page explains the privacy model and the browser/device-level limitation
- [x] 2026-03-04: Replaced the root `todo.md` refactor checklist with a concrete implementation plan for anonymous stream ratings in the schedule: one vote per stream per browser/device via `HttpOnly` cookie token hash, no auth, no raw IP storage, and explicit acceptance of browser-level rather than person-level uniqueness
- [x] 2026-03-04: Refined empty-day schedule logic: future days are now rendered as visually empty placeholders again, today without a started stream shows `Стрим еще не начался` until the end of the day, and only past days without a stream receive a deterministic random `Глеб Борисович ...` activity
- [x] 2026-03-04: Updated stream schedule copy: completed streams now show raw duration without the `Шел` prefix, and days without a stream render a deterministic random `Глеб Борисович ...` activity sourced from the new client-side activity list used by both desktop and mobile schedule views
- [x] 2026-03-04: Added `dependency-cruiser` project dependency map with `.dependency-cruiser.cjs`, `npm run deps:check`, and Windows-safe `npm run deps:graph` generation of `dependency-graph.svg` using Graphviz; baseline rules now flag UI imports of `src/server` and `src/db`, while current code passes the check
- [x] 2026-03-04: Normalized project root to real package identity `sasagram`, switched documented package manager to `npm`, removed in-repo `bun.lock`, added project README, and expanded `.gitignore` for local IDE/agent artifacts
- [x] 2026-03-04: Refactored `StreamSchedule` into `src/features/schedule/` modules (`components`, `lib`, `types`) while preserving the public component contract and visual behavior; kept `src/components/StreamSchedule.tsx` as a compatibility re-export
- [x] 2026-03-04: Refactored `TwitchVods` into `src/features/twitch/` modules (`components`, `lib`, `types`) while preserving pagination/UI behavior; kept `src/components/TwitchVods.tsx` as a compatibility re-export
- [x] 2026-03-04: Introduced `src/server/` layering for DB pool, stream repository/service, Twitch auth/media cache service, Kick helpers, and watch-also aggregation; `/api/streams`, `/api/twitch`, and `/api/watch-also` are now thin adapters
- [x] 2026-03-04: Moved shared browser fetch cache helper to `src/shared/lib/client-api-cache.ts` and left `src/lib/*` as compatibility re-exports for incremental migration safety
- [x] 2026-03-04: Updated `src/app/page.tsx` to consume feature entrypoints directly and pinned `turbopack.root` in `next.config.ts` to the repo root to avoid false workspace-root detection from lockfiles outside the repository
- [x] 2026-03-04: Verified refactor with `npm run lint`, `npm run typecheck`, and `npm run build`
- [x] 2026-03-04: Reviewed project structure; confirmed clear top-level split (`app` / `components` / `lib` / `db` / `scripts`) and identified main cleanup targets: oversized UI/API files, stale template metadata/docs, and mixed package-manager/tooling artifacts in repo root
- [x] Base Next.js 16 setup with App Router
- [x] TypeScript configuration with strict mode
- [x] Tailwind CSS 4 integration
- [x] ESLint configuration
- [x] Memory bank documentation
- [x] Recipe system for common features
- [x] Business card website template with 6 sections + Navbar + Footer
- [x] Twitch API integration: `/api/twitch` route fetches live status, VODs, followers
- [x] TwitchVods component: replaces Portfolio with real Twitch VODs grid
- [x] Hero: dynamic LIVE/OFFLINE badge from Twitch API
- [x] Contact: removed form, kept contact cards (Telegram, Email, Twitch, YouTube)
- [x] Navbar: "Клипы" → "Записи" (links to #vods)
- [x] next.config.ts: added `static-cdn.jtvnw.net` to image remotePatterns
- [x] Added rule: respond in Russian by default
- [x] Added MCP remote server: Context7 (configured in repo settings)
- [x] Removed "Контент" section from homepage and navbar navigation
- [x] Redesigned stream schedule in `About`: horizontal timeline with future scroll
- [x] Added schedule behavior: today auto-positioned at end of visible list
- [x] Added persistence of real stream start time (e.g. 18:18) via localStorage
- [x] Updated Twitch schedule API usage to request from UTC midnight (`start_time`) per docs
- [x] Timeline adjusted to past-only mode: today is always last card, no future days
- [x] Added PostgreSQL persistence for stream sessions (`started_at`, `duration_hours`)
- [x] Added setup script `npm run db:setup` for DB/table initialization
- [x] Added API route `/api/streams` (GET list, POST create)
- [x] About timeline switched from localStorage to PostgreSQL-backed persistence
- [x] Added automatic sync: live stream duration updates + VOD duration backfill into DB
- [x] Timeline cards now show `start - end`, total duration, stream title, and open stream URL on click
- [x] Schedule card switched to current-day mode only (no historical ribbon)
- [x] Added Twitch import script for all archived streams since 2026-01-01
- [x] Stream schedule moved to standalone section (`#schedule`) and restored 28-day timeline
- [x] Schedule list auto-scrolls so current day is always visible at the end
- [x] Schedule redesigned to weekly groups (scrollable week blocks for scaling)
- [x] Weekly schedule switched to single-week calendar view with Prev/Next navigation
- [x] Reworked weekly schedule into timeline calendar layout (time axis + day rows)
- [x] Fixed week calendar edge cases: 7-day week always visible, future days empty, half-hour axis, auto-refresh when stream starts
- [x] Switched project DB config to Neon (Vercel integration env vars)
- [x] Updated DB helpers/scripts to prefer `DATABASE_URL`/`POSTGRES_URL` with Neon SSL-ready connection strings
- [x] Updated schedule controls: animated arrow nav buttons + date picker jump-to-week + date column format `DD.MM`
- [x] Reworked week picker into custom month calendar (hover highlight by week + selected week row) and updated arrow button visual style
- [x] Timeline precision improved: minute-level helper grid lines appear on row hover without extra labels
- [x] Time axis refined with ruler-like ticks: short minute marks and longer hour marks on hover
- [x] Reverted ruler ticks in time header; simplified to hover highlight for time cells
- [x] Added hover highlight on schedule rows for better focus while scanning timeline
- [x] Updated week nav arrow buttons animation: button lifts on hover and presses on click, icon itself stays static
- [x] Fixed Twitch iframe embed for custom domains by building dynamic `parent` params from current hostname + fallback domains
- [x] Navbar logo switched to animated head MP4 (`sasych_head.mp4`) with blend-mode background suppression
- [x] Navbar logo video now prefers transparent `webm` source with MP4 fallback
- [x] Added canvas chroma-key processing for navbar logo video to remove green background in-browser
- [x] Navbar logo enlarged and animation changed to ping-pong playback (forward then reverse)
- [x] Reverted ping-pong behavior for navbar logo; restored normal loop playback (kept larger size)
- [x] Switched navbar logo asset to `sasavot_logo.webm` and removed canvas chroma-key pipeline
- [x] Navbar logo updated to `sasavot_logo_v2.webm` with fallback to previous logo file
- [x] Added 5-second delay before navbar logo animation restarts after each playback end
- [x] Logo playback changed to click-triggered mode only (no autoplay/loop)
- [x] Live Twitch player moved to Hero (replaces avatar while live); removed live player block from VODs section
- [x] Added floating picture-in-picture style live player that stays visible while scrolling below Hero
- [x] Reworked live PiP behavior to reuse the same Hero player (no duplicate iframe), with smooth transition to edge on scroll
- [x] Added PiP controls in floating mode: resize (+/-) and close button for the live player
- [x] Upgraded PiP controls: free drag-and-drop positioning + edge resize by pointer drag + close action
- [x] PiP UX refined: removed Move button, close now fully unmounts stream while floating, added all-side/corner resize handles, preserved custom PiP size across scroll cycles
- [x] Enhanced live state in schedule timeline with animated glow/sweep inside active stream block
- [x] Redesigned live schedule animation to smooth seamless flow (no visible hard reset)
- [x] Tuned live schedule animation speed/performance for smoother continuous motion
- [x] Reverted live animation tuning to previous seamless baseline after speed/seam regressions
- [x] Simplified live block motion to smooth shimmer (perelivanie) effect
- [x] Navbar restyled as floating island; logo moved outside navbar and enlarged
- [x] Navbar top offset tightened upward to add more gap from Hero live player
- [x] "Смотреть" button moved outside navbar; nav island now hugs only nav controls
- [x] Contacts section redesigned to circular items with avatar badges; links updated (YouTube clips, TG, Discord, @uran_mod)
- [x] Removed "Открыт для коллабораций" block in contacts and added TG link `PRAVILATIKTOKPREDLOSHKA`
- [x] Removed TG contact `PRAVILATIKTOKPREDLOSHKA` from contacts section
- [x] Contacts renamed to "Полезные ссылки" and new "Смотреть также" subsection added with Twitch/Kick channels
- [x] "Смотреть также" updated to show channel avatars (Twitch avatars + Kick fallback avatar)
- [x] Updated Roma's Twitch link in "Смотреть также" to `twitch.tv/r4dom1r`
- [x] Replaced proxy Twitch avatar links with direct `static-cdn.jtvnw.net` avatar URLs for stable rendering
- [x] Added dynamic Twitch avatar API (`/api/twitch/avatar/[login]`) and switched "Смотреть также" Twitch avatars to dynamic sources
- [x] Added dynamic Kick avatar API (`/api/kick/avatar/[slug]`) based on Kick public API endpoints with graceful fallback
- [x] Added live-status aggregator API (`/api/watch-also`) and creator tiles now show nicknames + LIVE/OFF states
- [x] Kick API auth upgraded: client credentials (`KICK_CLIENT_ID` + `KICK_CLIENT_SECRET`) now used for dynamic avatar/live fetches
- [x] Fixed Kick data mapping to use documented query endpoints (`channels?slug`, `users?id`) so live state and avatar resolve correctly
- [x] Next Image config updated to allow Kick CDN hosts (`files.kick.com`, `images.kick.com`)
- [x] Added real names beside creator nicknames in "Смотреть также" tiles
- [x] Contact avatars switched to platform/source avatars (YouTube, Telegram, Discord invite icon) and suggestion card now uses `alert_orig` logo video
- [x] Suggestion avatar video playback changed to hover-triggered mode only
- [x] Discord contact avatar switched to local logo asset `ds_logo.jpg`
- [x] Hero live layout adjusted: removed extra side text, centered enlarged player as primary above-the-fold element during live
- [x] Hero background height fixed to full viewport in live mode (`min-h-screen`)
- [x] All main homepage sections updated to viewport-height layout (`min-h-screen`)
- [x] Twitch chat embed integrated next to live player (desktop side-by-side, mobile stacked)
- [x] Added Twitch-like chat collapse toggle on live player block (show/hide chat)
- [x] Fixed schedule live mapping to use stream start date row (including cross-midnight live sessions)
- [x] Schedule time grid switched to hourly columns for cleaner vertical alignment
- [x] Schedule timeline header aligned to same hourly divisions as body grid (single shared column split)
- [x] Schedule timeline range made dynamic: auto-extends right boundary when streams run later into night
- [x] VOD cards enlarged by widening section container and shifting to 2-column layout until very wide screens
- [x] VOD section redesigned into viewport-height horizontal rows with arrow scrolling; added second row for Twitch featured clips
- [x] Full visual redesign of VOD/Clips shelves with cinematic overlay titles directly on thumbnails and upgraded glass-gradient styling
- [x] Tuned VOD/Clips shelf density: smaller banners, hidden scrollbars, and added compact "Все видео" Twitch button
- [x] Increased VOD fetch size and moved row arrows to Twitch-style side overlays; "Все видео" moved to row action area
- [x] Added tactile press animation to VOD controls (arrow buttons + "Все видео" button)
- [x] Reworked VOD/Clips into vertically aligned paged shelves with fixed visible card count and explicit prev/next pagination buttons
- [x] Hero content refreshed and live-player presentation enhanced (larger in-hero size during live)
- [x] Added PostgreSQL media cache for Twitch VODs/clips (`twitch_vods`, `twitch_clips`, `app_cache_state`) and switched `/api/twitch` to DB-first media reads with TTL-based sync
- [x] Added Supabase migration support: DB URL fallbacks for Supabase and script `db:migrate-to-supabase` for full table copy
- [x] Migrated core PostgreSQL data to Supabase via MCP (`streams`, `twitch_vods`, `twitch_clips`, `app_cache_state`)
- [x] Added project-level OpenCode MCP config for Supabase remote server (`opencode.json`)
- [x] Cleaned local env keys: removed legacy Neon/Vercel-only variables and kept active Supabase + API credentials only
- [x] Hardened DB connection parsing: auto-enforces `sslmode=require` for Supabase/Neon URLs when missing
- [x] Fixed false-positive Kick live status in `/api/watch-also` by removing object-truthy live detection and relying on nested `is_live`/status values
- [x] Fixed production video decode failures by removing Git LFS tracking for `public/assets/logo` static media
- [x] Excluded oversized local disclaimer sources (`дис2.mp4`, `дисклеймер4к.mp4`) from git via `.gitignore` and removed `дис2.mp4` from tracked files
- [x] Disclaimer video source is now configurable via `NEXT_PUBLIC_DISCLAIMER_VIDEO_URL` with local fallback `alert_orig.mp4`
- [x] Removed broken `<link rel="preload" as="video">` hint from root layout and fixed hydration mismatch in `DisclaimerOverlay`
- [x] Updated startup disclaimer default asset to local `дисклеймер_final.webm` with dynamic MIME type handling (`webm`/`mp4`) in `DisclaimerOverlay`
- [x] Disclaimer now switches by breakpoint: mobile uses `дисклеймен_final_mob.webm`, desktop keeps `дисклеймер_final.webm` (with optional env overrides)
- [x] Disclaimer preloader updated: label changed to `Загрузка`, and playback now starts only after both video readiness and full page `window.load`
- [x] Disclaimer preloader now shows real numeric progress (`%`) from combined page load + video buffering; `autoPlay` disabled to prevent premature start
- [x] Removed Next Image CDN dependency for Twitch previews/avatars: VOD/clip cards now use unoptimized image loading and `watch-also` API returns local avatar proxy routes
- [x] Removed `static-cdn.jtvnw.net` from Next Image remote patterns to avoid CDN optimizer path
- [x] Unified Hero background video source: both live and offline states now use `фон_сайт_онлайн.webm`; legacy `фон_сайт.webm` removed from runtime usage
- [x] Added fixed/sticky video background for all post-Hero sections using `фон_остальные_разделы.webm`; on scroll only section content moves
- [x] Increased fill contrast for VOD/Clips shelf controls (arrows + action buttons) to improve visibility on red patterned backgrounds
- [x] Removed heavy global background glow layers/animations (`site-dark-glow` pseudo-elements with blur/animation) to reduce render/GPU load and improve scroll smoothness
- [x] Reworked VOD section viewport sizing and stacking: `#vods` now uses fixed viewport-aligned container heights, stronger top separator, and raised stacking context to prevent visual overlap with schedule section
- [x] Fixed `#vods` top anchor offset by removing scroll margin (`scroll-mt-0`) so recordings section starts from the very top edge without exposing previous section
- [x] Adjusted Hero vertical layout: replaced center alignment with top alignment and set explicit navbar-to-content spacing of 20px for text/avatar block
- [x] Updated Hero background playback during disclaimer: removed disclaimer-visibility pause gate so Hero background video can render/load while disclaimer is playing; switched Hero background preload to `auto`
- [x] Removed section divider lines: disabled `blood-divider` pseudo-line and removed top/between-section borders from Schedule, VODs, and Footer wrappers
- [x] Unified post-Hero section darkening: About, Schedule, VODs, Contact, and Footer now use consistent overlay `bg-black/45` to eliminate visible tone shifts between sections
- [x] Fixed Kick avatar rendering in "Смотреть также": `next/image` optimization is now bypassed for `/api/*` avatar sources (same as remote URLs) to avoid optimizer issues with redirecting API image endpoints
- [x] Improved "Смотреть также" UX: removed upward hover shift from creator avatars (scale-only hover to prevent top clipping) and added mobile-first 2-column card layout with larger readable labels
- [x] Mobile navbar alignment updated: burger control moved from centered island position to top-right corner; mobile dropdown menu now right-aligned with compact max width
- [x] Stream schedule now has dual UX by breakpoint: mobile (`< md`) uses compact day cards with weekly navigation, while desktop/tablet (`md+`) preserves timeline grid layout
- [x] Mobile schedule cards compacted to fit full week view better: reduced paddings/spacings/font sizes and merged time+duration into a denser single row
- [x] Removed "Смотреть на Twitch" CTA from mobile navbar dropdown menu per UI cleanup request
- [x] Added local `Type Light Sans` font and applied it to schedule calendar date rendering (week range label + date cells) for visual consistency
- [x] Tuned mobile week-range label in schedule header: larger centered `Type Light Sans` text with explicit `3px` top/bottom padding
- [x] Increased mobile week-range number size by an additional 5px (`text-[21px]`) for stronger readability
- [x] Disabled horizontal page drift on mobile by enforcing `overflow-x: hidden` on both `html` and `body`
- [x] Removed mobile schedule separator lines: dropped outer border and internal bottom borders in the mobile week header block for cleaner continuous panel look
- [x] Matched post-Hero fixed background behavior on mobile with desktop: mobile now uses a dedicated `fixed` background layer (`md:hidden`) while desktop keeps sticky parallax layer (`md:block`)
- [x] Restored Hero background visibility after mobile fixed backdrop change by increasing Hero section stacking order (`z-20`)
- [x] Final mobile QA pass applied: tightened Hero headline/avatar scale for narrow screens, made VOD/Clips header controls wrap cleanly, reduced mobile side paddings in VOD section, and enabled footer link/copyright wrapping
- [x] Fixed Hero mobile avatar distortion by enforcing square sizing (`size-*` + `aspect-square` + `shrink-0`) on the avatar frame to prevent oval stretching

## Current Structure

| File/Directory | Purpose | Status |
|----------------|---------|--------|
| `src/app/page.tsx` | Home page — assembles all sections | ✅ Ready |
| `src/app/layout.tsx` | Root layout with SEO metadata | ✅ Ready |
| `src/app/globals.css` | Global styles (Tailwind import) | ✅ Ready |
| `src/components/Navbar.tsx` | Fixed navbar with mobile menu | ✅ Ready |
| `src/components/Hero.tsx` | Hero section with avatar, CTA, social links | ✅ Ready |
| `src/components/About.tsx` | About section with stats and resume download | ✅ Ready |
| `src/components/Skills.tsx` | Legacy content block (not rendered on homepage) | 💤 Unused |
| `src/components/Portfolio.tsx` | Portfolio grid with 6 project cards | ✅ Ready |
| `src/components/Contact.tsx` | Contact form with info cards | ✅ Ready |
| `src/components/Footer.tsx` | Footer with copyright and social links | ✅ Ready |
| `src/app/api/streams/route.ts` | Stream sessions API (read/write PostgreSQL) | ✅ Ready |
| `src/app/api/twitch/route.ts` | Twitch API with DB-cached VOD/clip sync | ✅ Ready |
| `src/lib/db.ts` | PostgreSQL connection pool helper | ✅ Ready |
| `scripts/setup-db.mjs` | Initializes DB tables (`streams`, `twitch_vods`, `twitch_clips`, `app_cache_state`) | ✅ Ready |
| `scripts/migrate-to-supabase.mjs` | Migrates streams/media/cache data to Supabase Postgres | ✅ Ready |
| `.kilocode/` | AI context & recipes | ✅ Ready |

## Website Sections

1. **Navbar** — Floating island nav with separate enlarged logo and mobile hamburger menu
2. **Hero** — Full-screen section with avatar, name, title, CTA buttons, social icons, scroll indicator
3. **About** — Two-column layout with photo placeholder, bio text, stats grid, resume download
4. **Twitch VODs** — Latest archived broadcasts with live/offline states
5. **Contact** — Contact info cards
6. **Footer** — Simple footer with copyright and social links

## Design System

- **Color scheme**: Dark (gray-950 / gray-900 backgrounds), Indigo/Purple accents
- **Typography**: Geist Sans (headings), Geist Mono (code)
- **Smooth scroll**: Enabled via `scroll-smooth` on `<html>`
- **Responsive**: Mobile-first, breakpoints at `sm:`, `md:`, `lg:`

## Customization Points

To personalize the template, update:
- Name: `Alex Johnson` → your name (in `Hero.tsx`, `Footer.tsx`, `layout.tsx`)
- Title: `Full-Stack Developer & UI/UX Designer` → your title (`Hero.tsx`)
- Avatar: Replace the letter placeholder with `<Image>` in `Hero.tsx` and `About.tsx`
- Stats: Update numbers in `About.tsx`
- Skills: Update skill names and percentages in `Skills.tsx`
- Projects: Replace project data array in `Portfolio.tsx`
- Contact info: Update email, location, phone in `Contact.tsx`
- Social links: Update `href` values in `Hero.tsx`, `Footer.tsx`

## Session History

| Date | Changes |
|------|---------|
| Initial | Template created with base setup |
| 2026-02-26 | Business card website template implemented with 7 components |
| 2026-02-27 | Set default response language to Russian; added Context7 MCP server config |
| 2026-02-27 | Removed "Контент" section; rebuilt schedule timeline with future horizontal scroll and persisted factual start time |
| 2026-02-27 | Schedule timeline switched to history mode: scroll only to past broadcasts, today fixed as last |
| 2026-02-27 | Added PostgreSQL DB setup and `/api/streams` for storing stream start time and duration |
| 2026-02-27 | Replaced browser localStorage persistence with PostgreSQL sync from live stream + VOD data |
| 2026-02-27 | Current-day schedule card + imported Twitch archives from start of 2026 into PostgreSQL |
| 2026-02-27 | Switched environment and DB scripts to Neon on Vercel (`DATABASE_URL`/`POSTGRES_URL`, pooled + non-pooled) |
| 2026-02-27 | Schedule nav updated with arrow buttons hover animation, week date picker, and date display as `23.02` |
| 2026-02-27 | Custom week calendar styled like compact datepicker: row hover/select for weeks, refreshed arrows, removed native date clear affordance |
| 2026-02-27 | Added minute-scale vertical helper lines in schedule rows that fade in on hover (labels unchanged) |
| 2026-02-27 | Time header now shows watch-like ticks on hover: short minute marks plus longer hour marks |
| 2026-02-27 | Removed watch-like ticks from time header and kept only clean hover highlight on hour cells |
| 2026-02-27 | Added hover background highlight for each schedule day row |
| 2026-02-27 | Week navigation arrows now animate as button lift/press only (no icon side movement) |
| 2026-02-27 | Twitch live embed now appends runtime hostname to `parent` list to avoid browser/Twitch frame blocking on non-vercel domains |
| 2026-02-27 | Replaced top-left text logo with animated head MP4 logo and blend-mode based background cleanup |
| 2026-02-27 | Updated navbar logo video sources to use `sasych_head.webm` first (alpha-capable) with MP4 fallback |
| 2026-02-27 | Implemented runtime chroma key on navbar logo video via hidden video + canvas to cut green screen |
| 2026-02-27 | Increased navbar logo size and implemented forward/reverse loop behavior for logo animation |
| 2026-02-27 | Removed ping-pong logo playback and switched back to standard looping animation |
| 2026-02-27 | Replaced logo source with `sasavot_logo.webm` and simplified navbar logo rendering to direct video |
| 2026-02-27 | Switched navbar logo source to `sasavot_logo_v2.webm` and kept old logo as fallback source |
| 2026-02-27 | Navbar logo video now waits 5 seconds after ending before replaying |
| 2026-02-27 | Navbar logo now starts animation only when user clicks on the head/logo |
| 2026-02-27 | Hero now shows embedded live stream instead of avatar during live; VODs section no longer embeds live player |
| 2026-02-27 | While live, a fixed mini-player appears on scroll (PiP style) and stays visible across page sections |
| 2026-02-27 | Live player now transitions from Hero position into PiP mode as the same element instead of spawning a second player |
| 2026-02-27 | Floating PiP player now supports manual resize and close actions from inline controls |
| 2026-02-27 | Floating PiP player can now be dragged anywhere and resized by dragging its right edge |
| 2026-02-27 | PiP player now resizes from any side/corner, shows only close button, unmounts stream when closed, and keeps user-defined size when returning to PiP |
| 2026-02-27 | Added animated visual activity for live schedule blocks (pulse glow + moving light sweep) to make "В эфире" state more dynamic |
| 2026-02-27 | Replaced harsh sweep loop with seamless striped flow + soft breathing overlay for cleaner live block motion |
| 2026-02-27 | Increased flow animation speed and optimized background-position animation path for smoother rendering |
| 2026-02-27 | Rolled back live flow speed/offset tweaks to the earlier stable seamless configuration |
| 2026-02-27 | Replaced striped live motion with a clean gradient shimmer animation for smoother visual flow |
| 2026-02-28 | Redesigned navbar into floating island style and moved/enlarged logo outside the nav container |
| 2026-02-28 | Increased vertical spacing between floating navbar and Hero player by moving navbar island higher |
| 2026-02-28 | Moved desktop "Смотреть" button out of nav island and made nav width fit only its own controls |
| 2026-02-28 | Reworked contacts into circular icon+avatar layout and replaced links with YouTube clips, Telegram channel, Discord invite, and TG @uran_mod |
| 2026-02-28 | Deleted collaboration availability card from contacts and added Telegram suggestion link `t.me/PRAVILATIKTOKPREDLOSHKA` |
| 2026-02-28 | Removed Telegram suggestion link `t.me/PRAVILATIKTOKPREDLOSHKA` from contacts |
| 2026-02-28 | Reframed contacts section into "Полезные ссылки" and added "Смотреть также" list with external Twitch and Kick creators |
| 2026-02-28 | Added avatar-based tiles in "Смотреть также" using Twitch avatar endpoints and Kick platform fallback image |
| 2026-02-28 | Switched Roma channel URL/avatar source to `https://www.twitch.tv/r4dom1r` |
| 2026-02-28 | Fixed missing avatars for several Twitch channels by switching from proxy avatar URLs to direct Twitch CDN image URLs |
| 2026-02-28 | Implemented dynamic Twitch avatar endpoint with redirect + caching and connected creator tiles to `/api/twitch/avatar/<login>` |
| 2026-02-28 | Implemented dynamic Kick avatar endpoint and connected `helin139ban` tile to `/api/kick/avatar/helin139ban` |
| 2026-02-28 | Added `/api/watch-also` to fetch Twitch/Kick live states and display LIVE/OFF badges with dynamic nickname metadata in creator list |
| 2026-02-28 | Added Kick OAuth client-credentials token flow in API routes so Kick avatar/status resolve with authenticated requests |
| 2026-02-28 | Corrected Kick API integration: switched from non-working path endpoints to working query endpoints and wired avatar/live extraction via `broadcaster_user_id` |
| 2026-02-28 | Added Kick image host allowlist in `next.config.ts` to fix `next/image` runtime error for Kick avatars |
| 2026-02-28 | Updated creator tiles to show provided real names next to nicknames (Ростик, Вероника, Танк, Витя, Нарек, Рома, Юра, Кирилл/Альфредо) |
| 2026-02-28 | Replaced static initials in top useful links with dynamic/source avatars; added Discord invite avatar API and enabled `alert_orig.mp4` as proposal logo avatar |
| 2026-02-28 | Updated suggestion logo behavior: `alert_orig.mp4` now plays only on hover and resets on mouse leave |
| 2026-02-28 | Replaced Discord avatar source with local `public/assets/logo/ds_logo.jpg` per design request |
| 2026-02-28 | Refined live Hero composition so stream player is the primary first-screen content (not fullscreen), removed side live message, and hid non-essential intro blocks while live |
| 2026-02-28 | Updated Hero live section height from `min-h-[86vh]` to `min-h-screen` to keep background filling full viewport |
| 2026-02-28 | Applied `min-h-screen` to About, Schedule, VODs, and Contact sections so each homepage block occupies at least one full viewport height |
| 2026-02-28 | Added Twitch chat embed in Hero live block using Twitch Embed Chat URL with `parent` params; chat appears beside stream on desktop and below on mobile |
| 2026-02-28 | Implemented chat visibility toggle in Hero live embed so users can collapse/expand Twitch chat similar to native Twitch layout |
| 2026-03-01 | Corrected schedule live-row logic: active stream now binds to its actual start date key instead of only "today" key, fixing missing live block for streams that started before midnight |
| 2026-03-01 | Changed schedule axis slot step from 30 to 60 minutes so grid columns align by full hours |
| 2026-03-01 | Fixed schedule header/body misalignment by rendering header on `axisTicks.length - 1` columns to match timeline lane segmentation |
| 2026-03-01 | Implemented dynamic schedule end time based on latest stream block in selected week (hour-rounded with buffer, capped) so late-night streams are fully visible |
| 2026-03-01 | Increased VOD tile visual size by setting section wrapper to full width (`w-full`, wider max width) and using `md/2xl` breakpoints (`2 cols` on most desktops, `3 cols` only on very wide screens) |
| 2026-03-01 | Rebuilt Twitch VOD area as fixed-height two-row media shelf (horizontal scroll + left/right controls) and extended `/api/twitch` with clips feed to power a new "Избранные клипы" row |
| 2026-03-01 | Restyled media shelves with stronger art direction: card titles moved onto video frames, larger type, cinematic gradient overlays, custom shelf accents, and refreshed control styling |
| 2026-03-01 | Reduced media card footprint to improve fit, removed visible horizontal scrollbars, and added quick-link chip to `https://www.twitch.tv/sasavot/videos?filter=all&sort=time` |
| 2026-03-01 | Expanded Twitch VOD API `first` limit from 6 to 20, repositioned media shelf arrows to overlay side controls (Twitch-like), and moved the "Все видео" action into the recordings shelf header |
| 2026-03-01 | Implemented physical button press feel for media shelf controls using active-state Y-offset and compressed shadow |
| 2026-03-01 | Replaced free horizontal-scroll shelves with page-based layout: cards are aligned in equal vertical grid columns, number of visible cards is capped per breakpoint, and rows navigate via dedicated prev/next buttons |
| 2026-03-01 | Added DB-backed Twitch media caching layer: new tables for VODs/clips + cache state, setup script updated, and `/api/twitch` now serves media from PostgreSQL with periodic Twitch sync |
| 2026-03-01 | Added Supabase migration path: DB helpers now accept `SUPABASE_DB_URL`/`SUPABASE_DATABASE_URL`; created `db:migrate-to-supabase` script for `streams`, `twitch_vods`, `twitch_clips`, `app_cache_state` |
| 2026-03-01 | Completed Supabase MCP data migration and verified counts: streams=52, twitch_vods=20, twitch_clips=20, app_cache_state=1 |
| 2026-02-28 | Reworked Hero text blocks and made live player significantly larger in Hero with stronger visual emphasis while stream is active |
| 2026-03-01 | Added `opencode.json` with Supabase MCP remote server configuration; attempted `opencode mcp auth supabase`, but local OpenCode binary crashed in this environment (Bun/JSC assertion) |
| 2026-03-01 | Removed unused keys from `.env.local`; dropped legacy Neon and Vercel-only tokens, leaving Supabase DB URL + active Twitch/Kick credentials |
| 2026-03-01 | Updated `src/lib/db.ts` to normalize env connection strings and append `sslmode=require` for Supabase/Neon hosts when absent |
| 2026-03-01 | Corrected Kick online/offline detection in `/api/watch-also`: nested stream objects no longer auto-mark channels as live without explicit live flag |
| 2026-03-02 | Added mandatory fullscreen disclaimer pre-roll (`/assets/logo/дисклеймер.mp4`) on homepage start: blocks page interaction, has no skip action, and closes only after video end; includes autoplay fallback prompt and retry handling on load error |
| 2026-03-02 | Switched startup disclaimer source to 4K asset (`/assets/logo/дисклеймер4к.mp4`) |
| 2026-03-02 | Added dedicated preloader screen for startup disclaimer and `<link rel="preload" as="video">` hint in root layout so disclaimer video is rendered only after readiness |
| 2026-03-02 | Hero background now uses video assets by live state: `фон_сайт_онлайн.webm` plays once when live, `фон_сайт.webm` loops when offline, and background playback pauses when Hero section is out of viewport |
| 2026-03-02 | Replaced startup disclaimer asset with `дис2.mp4` and rotated Hero live/offline background videos by 180 degrees |
| 2026-03-02 | Updated disclaimer behavior to skip playback on browser page reload (`navigation.type === reload`), so disclaimer runs only on normal page entry |
| 2026-03-02 | Replaced Hero avatar with local circular video `Кружок_сасыч.webm` (autoplay loop, muted) |
| 2026-03-02 | Adjusted navbar avatar sizing/cropping to fit circular outline and hid left logo on initial page view (shows only after scroll) |
| 2026-03-02 | Improved avatar transition stability: navbar avatar now fully fills the circle, and Hero-to-navbar avatar flight uses rAF throttling with smoother scroll behavior and no visible jump |
| 2026-03-02 | Navbar transparency made native: added reusable glass classes with `backdrop-filter`/`-webkit-backdrop-filter` and applied them to nav island + mobile menu |
| 2026-03-02 | Aligned navbar vertical offset with left logo: header top and nav top margin now match logo top spacing on mobile and desktop |
| 2026-03-02 | Added centered Hero title "Глеб Борисович Орлов" above avatar with typewriter reveal animation and custom local font (`public/assets/font/Audex`) |
| 2026-03-02 | Reworked Hero title into looping typewriter cycle with accurate inline cursor: text now types and erases per-char, then switches to slow dark-blood `SASAVOT` using `Them People` font |
| 2026-03-02 | Updated Hero typewriter timing: both title slides now stay visible for 10 seconds each before erase phase starts |
| 2026-03-02 | Fixed `SASAVOT` cursor vertical alignment by adding font-specific cursor metrics/offset for `Them People` in typewriter header |
| 2026-03-02 | Prevented Hero avatar shift during `SASAVOT` render by fixing the typewriter title block height across text states |
| 2026-03-02 | Optimized Hero typewriter workload: animation state now pauses during active page scroll and while Hero is outside viewport, then resumes from current progress |
| 2026-03-02 | Added global dark animated glow layer on page background (`body::after`) with blurred red/burgundy gradients and low brightness for non-hero sections |
| 2026-03-02 | Reworked non-hero background glow to a stronger visible `main` layer (`.site-dark-glow::before`) with dark red animated blur so sections no longer look flat black |
| 2026-03-02 | Tuned global section background from heavy dark glow to frosted-window look: layered matte haze + soft condensation highlights with subtle motion, keeping palette muted and non-bright |
| 2026-03-03 | Fixed media loading on production: removed LFS from public logo videos, deleted tracked `дис2.mp4` (kept local), moved disclaimer source to `NEXT_PUBLIC_DISCLAIMER_VIDEO_URL` fallback, removed invalid video preload hint, and resolved disclaimer hydration mismatch (`React #418`) |
| 2026-03-03 | Switched default startup disclaimer to `public/assets/logo/дисклеймер_final.webm` and made `<source type>` dynamic based on file extension to keep env override compatibility |
| 2026-03-03 | Disabled Next Image optimization for Twitch media cards, switched `watch-also` dynamic avatars to local `/api/*/avatar` routes, and removed `static-cdn.jtvnw.net` from `next.config.ts` image allowlist |
| 2026-03-03 | Hero background switched to single asset `фон_сайт_онлайн.webm` for both live/offline branches; `фон_сайт.webm` no longer referenced in app code |
| 2026-03-03 | Wrapped sections after Hero in a sticky video background layer (`фон_остальные_разделы.webm`) so background stays fixed while About/Schedule/VODs/Contact/Footer content scrolls on top |
| 2026-03-03 | Updated TwitchVods control styles: stronger solid fill and brighter border/text for prev/next arrows and `Все видео`/`Все клипы` buttons to improve readability |
| 2026-03-03 | Simplified global background rendering: removed animated glow/noise overlays and related keyframes from `globals.css`, keeping a lightweight static gradient backdrop |
| 2026-03-03 | Fixed recordings section intersection with schedule by resizing `#vods` to viewport-based heights (`calc(100vh - offset)`), increasing section paddings, adding a top border divider, and setting explicit `z-index` layering |
| 2026-03-03 | Removed top anchor offset for recordings (`scroll-mt-0` on `#vods`) to eliminate visible bleed of previous section when navigating to "Записи" |
| 2026-03-03 | Hero section top spacing recalibrated: switched to top-aligned layout and applied a 20px gap between navbar and the title/avatar content block |
| 2026-03-03 | Hero background video now preloads and can play behind the disclaimer overlay (no pause tied to disclaimer visibility), reducing the visual delay when disclaimer ends |
| 2026-03-03 | Removed visible horizontal separators between sections by disabling global divider pseudo-element and dropping section-level top/between borders on Schedule/VODs/Footer |
| 2026-03-03 | Standardized section backdrop opacity across all non-hero sections by aligning wrappers to `bg-black/45`, including replacing the custom radial VOD backdrop and dark Footer tone |
| 2026-03-03 | Resolved missing Kick avatar tile by bypassing Next image optimizer for local `/api/*` avatar URLs in `Contact` (direct browser fetch now handles API redirects consistently) |
| 2026-03-03 | Refined "Смотреть также" responsiveness and hover behavior: switched mobile layout to 2-column cards, increased avatar/text readability, and replaced translate-up hover animation with scale to remove avatar clipping |
| 2026-03-03 | Repositioned mobile burger navigation to the right corner by changing nav alignment (`ml-auto`) and right-aligning the mobile menu panel with a narrower max width |
| 2026-03-03 | Redesigned schedule behavior for phones: replaced horizontal time-axis matrix with vertical per-day cards plus prev/next week controls and "Сегодня" jump; desktop/tablet timeline remains unchanged |
| 2026-03-03 | Tightened mobile schedule card density (smaller rounded blocks, less vertical gap, compact typography) so all 7 days are visible on a typical phone viewport without heavy scrolling |
| 2026-03-03 | Deleted the extra Twitch CTA button from the mobile burger menu in `Navbar` to simplify the mobile header actions |
| 2026-03-03 | Integrated `Type Light Sans` (`TypeLightSans.otf`) via global `@font-face` and used `font-type-light-sans` in StreamSchedule calendar date UI (selected week range + month grid day numbers) |
| 2026-03-03 | Updated mobile week range display (`23.02 - 01.03`) to be more legible: increased font size, enforced center alignment with `flex-1`, and set vertical padding `py-[3px]` |
| 2026-03-03 | Bumped mobile week-range digits by +5px (from `text-base` to `text-[21px]`) while preserving center alignment and 3px vertical padding |
| 2026-03-03 | Applied global horizontal-overflow guard (`html, body { overflow-x: hidden; }`) to prevent sideways swipe/rotation-like layout shift on mobile devices |
| 2026-03-03 | Eliminated the visible "separator stroke" on phone schedule UI by removing mobile-only container/header borders in `StreamSchedule` |
| 2026-03-03 | Updated page background layering: added mobile-only fixed video backdrop for non-hero sections and kept desktop sticky backdrop, with content offset (`md:-mt-[100vh]`) only on desktop |
| 2026-03-03 | Fixed Hero background regression by elevating Hero section above shared fixed/sticky background layers (`z-20`), restoring dedicated Hero video rendering |
| 2026-03-03 | Completed targeted phone-width hardening (320–390px): Hero title lowered to `text-2xl` and avatar to `220px`, VOD/Clips controls now stack/wrap on mobile, container padding reduced to `px-3`, About heading scaled down on mobile, and Footer rows now wrap without overflow |
| 2026-03-03 | Corrected Hero avatar becoming vertically stretched on mobile by switching frame classes to unified `size-[220px] md:size-[276px]` with `aspect-square` and `shrink-0` |
| 2026-03-03 | Updated startup disclaimer source selection by viewport: `дисклеймен_final_mob.webm` is now used on mobile (`max-width: 767px`) while desktop stays on `дисклеймер_final.webm`; source auto-updates on breakpoint changes |
| 2026-03-03 | Updated disclaimer loading gate: preloader text is now `Загрузка`, and disclaimer playback waits for full site load (`window.load`) plus video readiness before starting |
| 2026-03-03 | Improved disclaimer UX: mobile rendering now uses adaptive fit (`object-contain` on phones), loading bar reflects real progress (page + video), and autoplay is disabled so playback begins only after full readiness |
- [x] 2026-03-03: Fixed phone disclaimer mismatch by exporting viewport metadata (`device-width`, `initialScale=1`, `viewportFit=cover`) from `src/app/layout.tsx`, preventing mobile browsers from resolving the startup disclaimer breakpoint in desktop mode
- [x] 2026-03-03: Optimized disclaimer startup loading by moving mobile/desktop source selection into native `<source media>` rules and reducing preload from `auto` to `metadata`, with readiness based on the first loaded frame instead of buffer-depth tracking
 - [x] 2026-03-04: Refined disclaimer preload UX in `DisclaimerOverlay`: waiting text changed to `Пожалуйста, подождите.`, the progress bar now reserves the final 5% for browser-side video preparation so `100%` appears only when playback can actually start, the `Загрузка` label has a soft pulse animation, and a warning now tells users not to refresh the page during loading
- [x] 2026-03-04: Simplified the About section in `src/components/About.tsx` by removing all streamer bio content, stats, and CTA buttons; section now shows a temporary `Раздел в разработке` placeholder
| 2026-03-03 | Fixed phone disclaimer mismatch by exporting Next.js viewport metadata (`device-width`, `initialScale=1`, `viewportFit=cover`) from `src/app/layout.tsx`, preventing mobile browsers from evaluating the disclaimer breakpoint in desktop mode |
| 2026-03-03 | Reduced disclaimer startup cost in `DisclaimerOverlay`: browser now picks the mobile/desktop asset through `<source media>`, video preload is `metadata` instead of `auto`, and readiness is based on `loadeddata` rather than buffered-duration tracking |
| 2026-03-03 | Hardened startup disclaimer stability: replaced `window.load` gating with DOM readiness (`DOMContentLoaded` / `document.readyState`) plus a 1.5s fallback timer, so the overlay cannot freeze waiting for unrelated late resources |
| 2026-03-03 | Reduced startup media contention: Hero background videos now stay paused while the disclaimer is visible and use `preload="metadata"`, preventing desktop/mobile stalls caused by concurrent heavy video loading |
| 2026-03-03 | Reworked startup disclaimer flow into explicit staged UX: device type is detected first, the selected video is downloaded via XHR with exact byte-based percentage progress, and playback starts only after the user presses `Start` |
| 2026-03-04 | Reviewed repository structure: top-level layering is sound for a small Next.js app, but future maintainability is constrained by oversized components/routes, outdated template naming/documentation, and duplicated package-manager/build artifacts at the repo root |

